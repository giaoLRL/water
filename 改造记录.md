# 改造记录

## 关键改动文件

**站点文案可编辑：浏览器标签标题 / 顶栏大标题 / 副标题（免改代码）**

| 文件 | 改动 |
| --- | --- |
| backend/api.py | 新增 GET /api/water/site（无需登录，登录页也展示；默认文案内置）与 POST（cfg_system，写 config.site 日志；存 config 表 site_title/site_subtitle/site_browser） |
| frontend/js/app.js | 顶栏 h1/副标题绑定全局响应式 site 对象；loadSite() 启动拉取并同步 document.title（browser 留空=同站点标题） |
| frontend/js/components/sysConfig.js | 告警阈值页签顶部新增「界面文案」编辑区（站点标题/副标题/浏览器标签标题，cfg_system）；保存后经 window.AppSite 即时同步顶栏与标签 |
| frontend/js/api.js | 新增 siteGet/siteSet |
| frontend/js/widgets.js | ACTION_LABELS 加 config.site=修改站点文案 |

## 关键改动文件（上一轮：自定义传感器通道全链路）

| 文件 | 改动 |
| --- | --- |
| backend/database.py | 新建 custom_sensor_data 表（channel_id+ts+value，idx_custom）+ insert/query_history/stats 三个函数 |
| backend/custom_channels.py | 新建 CustomChannelManager：通道声明加载、按各自周期轮询（白名单校验+JSON 取值路径）、有效值入库并维护实时值 |
| backend/main.py | 采集循环注册 services.custom 并每周期 poll()，读数并入 data["custom"] |
| backend/api.py | 新增 GET/POST /api/water/custom/channels（声明，cfg_system）、GET /api/water/custom/history|stats（view_history）；_snapshot 暴露 custom_channels/custom |
| backend/alarm.py | 联动触发源支持 kind=channel（自定义通道，取 data.custom[id]）；联动越限/恢复写 alarms 表（type=custom:<规则id>，告警记录页可见） |
| frontend/js/api.js | 新增 customChannelsGet/Set、customHistory、customStats |
| frontend/js/components/sysConfig.js | 报警联动区下方新增「自定义传感器通道」声明区（名称/URL/路径/轮询/单位，cfg_system）；联动触发源下拉加「自定义通道」 |
| frontend/js/components/waterDashboard.js | 历史页动态出现自定义通道标签（customHistory 取数画线）；统计页每通道均值/最高/最低/采样数指标卡 |
| backend/tests/test_api.py | 新增「9.4 自定义传感器通道」：声明/白名单/周期校验/权限/realtime 快照/历史/统计/日志/恢复 |
| tests/e2e_web.js | 新增「7.3c 自定义传感器通道」：声明区渲染/添加保存/接口回读/联动下拉含通道/恢复 |
| AGENTS.md / README.md | 同步全链路描述（数据模型、机制、接口表、验证清单） |

## 关键改动文件（上一轮：报警联动 v2 个体化）

| 文件 | 改动 |
| --- | --- |
| backend/alarm.py | 联动引擎重构 v2：规则自带阈值；触发源 builtin(本机通道，随 check 评估)/custom(URL+路径+轮询周期，check_custom 按各自周期轮询)；执行器档案 sys.actuators（名称+开/关 URL）；越限沿触发/回正常沿执行可选恢复动作（状态去重）；动作指向显式个体（本机水泵/加热/定量 或 档案执行器 on/off），写 control_log(link_*，source=auto) |
| backend/api.py | links v2 校验（sensor kind/通道/自定义源白名单/阈值数值/动作执行器引用存在）；新增 GET/POST /api/water/alarm/actuators 执行器档案端点（名称必填、开/关 URL 至少一条、白名单校验）；links GET 同时返回档案 |
| backend/main.py | 采集循环 alarm.check 后调 alarm.check_custom()（自定义源轮询） |
| frontend/js/components/sysConfig.js | 报警联动区个体化 UI：执行器档案编辑（名称/开URL/关URL）+ 规则编辑器（规则名、触发源二选一本机通道/自定义接口、阈值+方向、动作标签组显式执行器个体+开/关、可选恢复动作）；保存先档案后规则 |
| frontend/js/api.js | 新增 alarmActuatorsGet/Set；alarmLinksGet 返回含 actuators |
| frontend/js/widgets.js | ACTION_LABELS：link_custom → link_actuator |
| frontend/css/style.css | 新增 .link-rule 规则块样式 |
| backend/tests/test_api.py | 9.3 改 v2：执行器档案读写/白名单与空 URL 拦截/规则自带阈值/执行器引用与状态校验/日志/恢复 |
| tests/e2e_web.js | 7.3b 改 v2：建执行器档案(E2E水阀)→建规则(本机通道+阈值+关本机水泵)→接口回读→恢复空 |
| AGENTS.md / README.md | 同步 v2 个体化描述 |

## 关键改动文件（上一轮：报警联动 v1）

| 文件 | 改动 |
| --- | --- |
| backend/alarm.py | 联动引擎：加载/重载 sys.alarm_links 规则；check() 收集「新激活」告警，锁外按规则执行动作（off_pump/off_heater/cancel_target/custom URL 白名单校验+urllib 下发）；每动作写 control_log(link_*，source=auto)；失败仅记 fail 不抛异常 |
| backend/api.py | 新增 GET/POST /api/water/alarm/links（cfg_alarm 权限；校验通道/方向/动作类型/自定义 URL 白名单；保存后 reload_links 即时生效；写 config.link 日志） |
| backend/main.py | 启动时 services.alarm.plant = services.plant（联动动作执行对象注入） |
| frontend/js/components/sysConfig.js | 系统配置页新增「报警联动」编辑区：规则行（通道/方向下拉按 features 裁剪 + 动作标签组可增删 + custom URL 内联输入 + 启用开关），保存即生效 |
| frontend/js/api.js | 新增 alarmLinksGet/alarmLinksSet |
| frontend/js/widgets.js | ACTION_LABELS 加 config.link 与 link_off_pump/link_off_heater/link_cancel_target/link_custom（日志与最近操作卡共用） |
| backend/tests/test_api.py | 新增「9.3 报警联动」：读写/权限拦截/回读一致/非法通道方向动作 40002/白名单外 URL 40002/入日志/恢复 |
| tests/e2e_web.js | 新增「7.3b 报警联动」：编辑区渲染/添加规则(光照+超上限+关水泵)/保存后接口回读/恢复空 |
| AGENTS.md / README.md | 同步联动机制描述（触发语义、动作与审计、sys.alarm_links） |

## 关键改动文件（上一轮：仪表盘全开放）

| 文件 | 改动 |
| --- | --- |
| frontend/js/components/waterLoop.js | **删除**：循环回路 SVG 组件整体移除（含 index.html 引用与全部回路 CSS） |
| backend/api.py | dashboard/proxy：非 JSON 返回不再报错（返回 text 字段）；新增 log=1 参数 → 写操作日志 device.custom（自定义指令审计） |
| frontend/js/widgets.js | 删 loop 与 tank_storage/tank_heater 目录项；新增通用「单水槽」卡（multi:true 不判重，对象源/数值源双形态）；默认布局两张单水槽实例（x0/x4 w4h9，绑定 tank.tanks 对象保留角标）；ACTION_LABELS 加 device.custom |
| frontend/js/components/widgetShell.js | 删 LoopViz 注册与 loop 分支；tank 卡兼容数值源（角标「自定义」，无传感器 > 离线 > 实测/自定义）；toggleCtl 支持 cmd:{on,off} → 代理 GET 下发（log=1），留空走内置通道 |
| frontend/js/components/waterDashboard.js | 配置对话框统一「接口 URL」直填（留空=实时快照，无来源下拉），value/spark/line/state/tank/control 六卡通用；control 卡增开/关指令 URL 行（成对校验）；confirmCustom 支持全部六种卡型；serialize 加 cmd；catalogList 对 multi 卡不判重 |
| frontend/js/api.js | dashboardProxy(url, log) 第二参数触发代理审计日志 |
| frontend/index.html / css/style.css | 移除 waterLoop.js；版本号统一 v=10；删除回路专属样式（loop-*/tank-* 等），reduced-motion 改指水槽卡过渡 |
| tests/e2e_web.js | 删全部回路 SVG 断言；默认 11 卡 + .wg-tank×2 断言；水位一致性改读水槽卡数值；新增单水槽可重复添加、自定义控制卡（指令 URL 填只读安全地址，绝不点击）用例 |
| AGENTS.md / README.md | 同步：无循环回路系统、单水槽卡、直填 URL、cmd 指令与审计描述 |

## 关键改动文件（上一轮：全卡片化重构）

| 文件 | 改动 |
| --- | --- |
| frontend/js/widgets.js | CATALOG 增至 21 项：新增 loop/quant/pid/recent/reset 五张 builtin 特殊卡与 tank/control 卡型；目录项新增 perm（权限点）/flag（realtime 布尔）门控字段；defaultLayout 重排为显式坐标 14 行默认布局（flow/total w6、四小卡 w3、回路 w8h9 + 右列控制/定量卡）；targetProgress/actionLabel/isSystemLog/operatorText 下沉为共享纯函数 |
| frontend/js/components/widgetShell.js | 注册 WaterLoopViz；新增 loop/quant/pid/recent/reset 五个 builtin 渲染分支，操作卡逻辑自包含（自行拉取 PID 参数与最近操作、直调 pump/heater/pumpTarget/volumeReset 接口，与旧固定操作区同一权限点）；无权限/离线渲染禁用态而非移除；新增 tank/control 卡渲染 |
| frontend/js/components/waterDashboard.js | 三栏驾驶舱（.cockpit/.ck-loop/.ck-ops）与页头水泵/加热快捷开关整体移除，改为单一 .grid-zone 全宽网格；删除已迁入卡片的方法与状态；catalogList 过滤扩为 feature+perm+flag 三条件；loadLayout 按 builtin 去重防同类卡重复 |
| frontend/css/style.css | 删三栏布局/更多操作折叠/页头开关/kpi-duo 样式；新增 .grid-zone；卡内中和 op-card/loop-wrap 双层背景；.loop-svg 卡内 max-height:none；水槽卡与控制开关卡样式 |
| frontend/index.html | waterLoop.js 调至 widgetShell.js 之前（注册依赖）；版本号 v=9 |
| tests/e2e_web.js | 第 4/4.1/4.2/6/9 节断言重写：三栏→单网格、kpiExpected 新公式（默认 10 卡）、控制开关改读网格内 .wg-ctl、清零卡改为临时添加验证二次确认、新增编辑栏位置/定量脚注可见/清零卡不在默认布局断言；设备离线时自定义卡数值校验降级 WARN |
| AGENTS.md / README.md | 可编辑仪表盘段落与验证清单同步为全卡片化描述 |

## 关键改动文件（上一轮：可编辑仪表盘初版）

| 文件 | 改动 |
| --- | --- |
| frontend/js/lib/gridstack-all.js / frontend/css/gridstack.min.css | 新增：gridstack 11.5.1 本地 UMD 库（12 列网格拖拽/缩放） |
| backend/config.py | 新增 `DASHBOARD_PROXY_HOSTS` 代理白名单（默认本机/设备主机，`WATER_DASHBOARD_PROXY_HOSTS` 扩展） |
| backend/api.py | 新增仪表盘段：`GET/POST /api/water/dashboard/layout`、`POST .../layout/reset`（布局存 config 表 sys.dashboard_layout，全局共享）、`GET .../dashboard/proxy?url=`（仅 GET/JSON、白名单防 SSRF）；保存写 config.dashboard 日志 |
| frontend/js/widgets.js | 新增：`window.Widgets`——卡片目录 CATALOG（11 项内置）、defaultLayout(features)、resolvePath 取值、fromCatalog |
| frontend/js/components/widgetShell.js | 新增：通用卡片渲染组件（value/spark/line/state/builtin 五型，自定义源独立轮询，超阈值标红） |
| frontend/js/components/waterDashboard.js | 左栏 KPI 改造为 gridstack 网格；新增编辑模式（拖拽/缩放/删除/完成）、添加卡片对话框（内置目录 + 自定义接口页签带预览取值）、卡片配置对话框、布局防抖保存/恢复默认；原固定 KPI/趋势渲染方法移除 |
| frontend/js/api.js | 新增 dashboardLayoutGet/Save/Reset/dashboardProxy |
| frontend/index.html / css/style.css | 引 gridstack 库与 widgets/widgetShell；新增网格卡片、编辑态、目录与表单样式 |
| backend/tests/test_api.py | 新增「9.2 仪表盘布局与自定义卡片代理」：布局读写/权限拦截/代理白名单与代发验证 |
| tests/e2e_web.js | 新增「4.2 可编辑卡片仪表盘」：网格渲染/编辑模式/增删内置卡/自定义接口卡预览与显示/删除还原；用例前后快照并恢复布局 |

## 关键改动文件（上一轮：清除全部模拟数据）

| 文件 | 改动 |
| --- | --- |
| backend/config.py | 删除 `SIM_LEVEL_*` 全部模拟参数；`level_storage` 注释改为「无数据来源 → 恒为 0」；LEVEL_PATH_STORAGE 留空 |
| backend/water_device.py | 删除 `_simulate_levels()` 与 `_last_level_ts`；两槽水位初始为 0；`_read_level` 无路径/503/404 一律归 0（网络抖动保留上次值防闪烁）；`_mark_failed` 离线后两槽归 0 |
| backend/api.py | `_tank_info` 来源改 `device`/`none`，未接入槽位水位恒 0；`any_sim` → `any_unavailable` |
| frontend/js/components/waterLoop.js | 新增 `online` prop；「模拟」角标改为 `tagOf()`：未接入→「无传感器」、离线→「离线」 |
| frontend/css/style.css | `.tank-sim-tag` → `.tank-tag` |
| frontend/js/components/waterDashboard.js | `tankAnySim` → `tankUnavailable`；回路徽标改「设备离线／液位不完整／液位实测」并传 `online`；文案改为无模拟说明 |
| frontend/js/components/sysConfig.js | 液位说明改为「储水槽恒显示 0 并标注无传感器，已清除全部模拟数据」 |
| backend/tests/test_level.py | 新建：液位契约单测（无模拟初值/无 `_simulate_levels`/无 `SIM_*` 残留/未接入恒 0/离线归 0）；原 `test_level_sim.py` 已删除 |
| backend/tests/test_api.py | 断言改 `any_unavailable`；新增「储水槽 source=none 且水位恒 0」「离线两槽归 0」 |
| tests/e2e_web.js | 「模拟」角标断言改为「无传感器」+「无数据槽位显示 0%」 |
| AGENTS.md / README.md | 同步清除模拟表述，并注明该用户指令优先于原「不用 0 冒充真实读数」的表述（0 必须带 source=none 或离线标志） |

## 关键改动文件（上一轮：超声波液位 + 光照接入）

| 文件 | 改动 |
| --- | --- |
| backend/config.py | `DEVICE_FEATURES` 启用 level_heater / light（level_storage 保持 False）；`LEVEL_PATH_HEATER` 改为 `/api/level`、`LEVEL_PATH_STORAGE` 置空；`DEFAULT_THRESHOLDS` 加 light_max=2000/light_min=0 lx |
| backend/esp32_client.py | 新增 `light()`（503→None）；接口清单加 /api/level、/api/level/height、/api/light |
| backend/water_device.py | 新增 light 状态字段并在 read() 每周期采集；`_read_level` 空路径保护（储水槽跳过）；离线清理 light（液位保留最近读数）；status() 返回 light |
| backend/database.py | water_sensors 建 `light` 列 + 幂等迁移；insert/history/stats 均带 light 聚合（avg/max/min） |
| backend/alarm.py | CHANNELS 加光照告警（light_max/light_min，依赖 light 能力） |
| backend/main.py | ingest 覆盖字段加 light；入库传 light；DEBUG 日志加 lv/lx |
| backend/api.py | AlarmConfigRequest 加 light_max/min；IngestRequest 加 light；`_snapshot` 补 light 字段；告警阈值日志 detail 超 250 字符截断（修 500） |
| backend/database.py（追加） | `insert_control_log` 统一截断 detail 兜底（VARCHAR(255)，字段变多后防 1406） |
| frontend/js/charts.js | 修复 `init()` 残留已销毁实例的缺陷（dispose 后未清 registry，切页后 getDom() 为 null 报 TypeError） |
| frontend/js/components/waterDashboard.js（布局） | 光照卡 `.kpi-wide` 跨双列；移除水泵 KPI 卡（顶部开关与回路图已表达）与大卡 foot 阈值行（配置页/定量卡均有）；保持首屏零滚动 |
| frontend/css/style.css（布局） | 大卡 value 29→27、spark 32→26、栏 gap 10→8；loop-svg 自适应开销 270→300 |
| frontend/js/components/waterDashboard.js | KPI 加光照卡；历史曲线加光照标签；统计页加光照最高/最低卡；告警类型映射加"光照" |
| frontend/js/components/sysConfig.js | 阈值表单加光照上限/下限（lx，features.light 门控）；液位说明更新为"加热槽实测、储水槽模拟" |
| backend/tests/test_api.py | realtime 加 light 字段/能力断言；加热槽液位 source=device、储水槽 sim 断言；统计/告警阈值断言含光照 |
| backend/tests/test_level_sim.py | 液位通道断言改为"储水槽模拟、加热槽实测" |
| tests/e2e_web.js | KPI 卡动态断言加 light 计数 |
| AGENTS.md | 1.1 表加液位(超声波,1路,加热槽)/光照行；液位例外段改为"加热槽实测、储水槽模拟"；数据模型加 light 列 |

## 更新历史

- **2026-09-18（自定义传感器通道全链路）**：自定义传感器从"前端展示层"升级为后端全链路。系统配置页声明通道（URL/取值路径/轮询周期/单位，cfg_system），采集循环按周期轮询 JSON 取值并入库 custom_sensor_data 表；历史曲线页动态出现通道标签、统计页出现均值/极值/采样数指标卡；联动规则触发源可直接选"自定义通道"；联动越限/恢复沿写 alarms 表（告警记录页可见）。实时值经 realtime 快照（custom.<id>）供卡片显示。无数据不入库不做模拟。终测：test_api 141 通过 0 失败、e2e_web 145 通过 0 失败（设备离线 2 WARN 为设计内降级）。

- **2026-09-18（报警联动 v2 个体化）**：按用户反馈重构联动规则——「哪个传感器的阈值触发哪个执行器」须分清个体。规则自带阈值+方向；触发源支持本机 5 通道或自定义接口传感器（URL+取值路径+轮询周期，采集循环自动轮询）；新增执行器档案（sys.actuators：名称+开/关 GET URL，白名单校验），动作显式引用档案个体（本机水泵/加热/定量 或 执行器 on/off）；越限沿触发、回正常沿可选恢复动作（状态去重防重复）。系统配置页 UI 重做为执行器档案+规则编辑器。终测：test_api 131 通过 0 失败、e2e_web 139 通过 0 失败。

- **2026-09-18（报警联动）**：新增可编辑的「告警触发 → 设备动作」联动系统。规则 = 通道+方向+动作组（关水泵/关加热/取消定量/自定义 GET 指令 URL），存 config 表 sys.alarm_links，系统配置页「报警联动」区编辑（cfg_alarm 权限）。告警**新激活**瞬间在采集循环内自动执行（锁外，失败仅记 fail），每动作写 control_log(link_*，source=auto) 审计；自定义指令走代理白名单。保存即生效（reload_links），写 config.link 日志。历史/统计/告警/日志页签保持现状。终测：test_api 126 通过 0 失败、e2e_web 136 通过 0 失败（设备离线 2 WARN 为设计内降级）。

- **2026-09-17（仪表盘全开放）**：彻底删除循环回路系统（waterLoop.js 组件、回路卡与全部回路样式）。新增通用「单水槽」卡：接口/名称/颜色自由配置、对象源与数值源双形态、目录不判重可加任意多个，默认布局以两张单水槽卡（储水槽/加热槽）替代回路卡。全卡直填接口：六种卡配置对话框统一「接口 URL」一栏，留空读本机实时快照、填了走代理轮询。控制开关卡支持自定义开/关指令 URL（成对校验、经代理 GET 下发、写 device.custom 审计日志），留空用内置通道。添加卡片对话框支持全部六种卡型。代理放宽接受非 JSON 返回。终测：test_api 114 通过 0 失败（设备离线 10 WARN）、e2e_web 131 通过 0 失败 2 WARN，首屏 807px=视口零滚动。

- **2026-09-17（全卡片化重构）**：实时监控页整体卡片化。循环回路 SVG、定量浇水、恒温 PID、最近操作、清零累计全部变成网格内 builtin 卡（逻辑自包含于 widgetShell，直调后端接口、权限点不变）；新增 tank 迷你水槽卡与 control 控制开关卡；页头水泵/加热快捷开关与三栏固定布局移除。默认布局 14 行 10 卡，1366×768 首屏零滚动；清零卡不入默认布局（危险操作降权）；目录项支持 feature/perm/flag 三重门控，无权限渲染禁用态。旧布局兼容：已保存布局照常加载，点「恢复默认」得新组合。终测：test_api 120 通过 0 失败、e2e_web 132 通过 0 失败 2 WARN（现场设备运行中偶发离线的设计内降级）。

- **2026-09-17（可编辑仪表盘）**：实时监控页左栏改为 gridstack 可编辑卡片网格。卡片=JSON 配置（五型 value/spark/line/state/builtin），支持编辑模式拖拽/缩放/删除、内置卡片目录按设备能力添加、自定义 GET 接口卡（URL+取值路径+轮询周期，预览取值后经后端代理代发，主机白名单防 SSRF）。布局存 config 表 `sys.dashboard_layout`，全局共享、防抖即存、可一键恢复默认；保存需 cfg_system 权限并写 config.dashboard 日志。未保存布局时按设备能力生成默认布局（复刻原 7 张卡）。终测：test_api 114 通过 0 失败、e2e_web 136 通过 0 失败。

- **2026-09-16**：温度 ×2 / 压力 / 加热通道全量接入。后端采集、入库、告警、PID 闭环、接口；前端实时面板、历史曲线、统计、阈值配置、PID 操作。压力统一 kPa（固件 MPa ×1000）。PID 默认仍关闭（WATER_PID_ENABLED=0），界面可开启。硬件离线，未实测连接，待设备上电后按 AGENTS.md 验证清单复核。
- **2026-09-16（同日补）**：e2e 回归发现首屏超出视口 254px（新增 KPI/操作卡撑高左栏）。将温度 ×2/水压/水泵 4 张卡改为 `.kpi-duo` 2×2 双列紧凑布局并收窄间距，恢复首屏零滚动；e2e 卡片数量断言改为按固件能力动态计算。三套测试全绿：test_api 98 通过 0 失败、test_level_sim 全部通过、e2e_web 116 通过 0 失败（1 条设备离线 WARN）。
- **2026-09-16（第3次）**：固件新增超声波水位与光照通道。加热槽液位切实测（/api/level 百分比，储水槽保持模拟并标注）；光照全链路接入（采集/入库/告警阈值/KPI/历史/统计）。回归中发现并修复三处：① 告警阈值日志 detail 超 VARCHAR(255) 导致保存接口 500（统一截断兜底）；② charts.js 残留已销毁实例导致切页后 TypeError；③ 光照卡使首屏超视口（水泵 KPI 卡移除、光照卡跨双列、尺寸微调后恢复零滚动）。终测：test_api 102 通过 0 失败、test_level_sim 全部通过（断言改混合模式语义）、e2e_web 119 通过 0 失败。
- **2026-09-16（第4次·清除模拟数据）**：按用户要求删除全部模拟水位逻辑（`_simulate_levels`/`SIM_LEVEL_*`/`_last_level_ts`），无数据一律保持 0 并在界面提示原因（「无传感器」/「离线」）。接口 `source` 由 `sim` 改为 `none`，`any_sim` → `any_unavailable`；回路图角标与面板文案同步改写。原液位模拟单测改写为《液位通道契约测试》(`test_level.py`)。**设备已上电实测**：探头1 22.1℃、光照 34~57 lx、加热槽液位 0.0%、加热继电器 off 均正常回传（探头2 与压力暂无读数，字段留空不伪造）。终测：test_api 111 通过 0 失败、test_level 全部通过、e2e_web 120 通过 0 失败。
