# 物联网技能赛——AI 编程通用约束

你正在为“4小时现场限时、禁止联网、禁止使用AI辅助开发”的物联网技能赛编写代码。

代码的首要目标不是追求复杂、优雅或高度抽象，而是：

> **让没有较强手写代码能力的参赛者，也能够在现场快速看懂、定位、修改、组合和调试代码。**

因此，你必须严格遵守以下规则。

---

## 一、总原则

### 1. 优先考虑“可读、可改、可调、可排错”

代码必须满足：

1. 初学者能够快速理解；
2. 能通过搜索变量名快速定位功能；
3. 参数能够集中修改；
4. 每个功能尽量独立；
5. 出错时能够快速判断问题属于哪一层；
6. 现场修改时尽可能只修改少量代码；
7. 不允许为了“代码高级”而增加不必要的抽象。

不要为了代码优雅而增加复杂架构。

---

# 二、禁止过度工程化

除非任务明确要求，否则禁止主动引入：

- 复杂设计模式
- 工厂模式
- 依赖注入
- 大量类继承
- 复杂模板
- 过度封装
- 多层无意义抽象
- 大量宏嵌套
- 过度使用函数指针
- 复杂回调系统
- 不必要的动态内存
- 不必要的多线程
- 不必要的操作系统级架构
- 不必要的第三方库

嵌入式比赛代码应该：

> **简单、直接、稳定。**

如果一个功能可以用20行简单代码解决，不要设计成200行复杂框架。

---

# 三、必须采用分层结构

代码至少划分为以下几层：

```text
硬件底层
    ↓
设备驱动
    ↓
设备接口
    ↓
业务逻辑
    ↓
通信
```

推荐目录：

```text
project/
├── bsp/
├── drivers/
├── devices/
├── protocol/
├── application/
├── config/
└── main.c
```

---

# 四、底层代码和业务代码必须分离

这是最高优先级规则之一。

例如：

错误做法：

```c
GPIO_WritePin(PORTA, PIN3, GPIO_PIN_SET);
```

直接写在业务逻辑中。

正确做法：

```c
Pump_On();
```

业务层只允许调用：

```c
Pump_On();
Pump_Off();
Valve_Open();
Valve_Close();
Buzzer_On();
Buzzer_Off();
```

传感器也必须提供简单接口：

```c
float Temperature_Get(void);
float Humidity_Get(void);
float WaterLevel_Get(void);
float Flow_Get(void);
```

业务逻辑不得直接操作底层寄存器。

---

# 五、所有硬件模块必须提供“简单API”

每一个模块至少提供：

```c
xxx_Init();
xxx_Get();
xxx_Set();
xxx_On();
xxx_Off();
```

根据模块性质选择需要的接口。

例如：

```c
void Pump_Init(void);
void Pump_On(void);
void Pump_Off(void);

float Flow_Get(void);

int WaterLevel_Get(void);
```

函数名称必须让人一眼知道作用。

禁止使用毫无语义的函数名：

```c
proc();
handle1();
do_task();
func();
control();
```

除非名称具有明确上下文。

---

# 六、所有比赛参数必须集中管理

所有可能需要现场修改的参数必须集中放在：

```text
config.h
```

或者：

```text
config.c
```

例如：

```c
#define WATER_LOW_LEVEL      30
#define WATER_HIGH_LEVEL     80

#define FLOW_MIN              0.5
#define TEMPERATURE_LIMIT    35

#define SENSOR_PERIOD_MS     1000
#define UPLOAD_PERIOD_MS     1000
#define ALARM_DELAY_MS       3000
```

严禁把比赛参数散落在几十个文件中。

禁止：

```c
if(level < 30)
```

推荐：

```c
if(level < WATER_LOW_LEVEL)
```

---

# 七、现场可修改参数必须有明显标记

例如：

```c
/* =========================
 * 比赛现场需要修改的参数
 * ========================= */

#define WATER_LOW_LEVEL   30
#define WATER_HIGH_LEVEL  80
#define FLOW_LIMIT        0.5
```

并在注释中明确：

```c
// 【现场修改】水位下限
// 【现场修改】水位上限
// 【现场修改】流量报警阈值
```

---

# 八、代码必须使用统一、直观的变量名

统一使用：

```c
temperature
humidity
water_level
flow
light
distance

pump
valve
fan
lamp
buzzer

system_state
alarm_state
sensor_error
network_status
```

禁止一个工程中同时出现：

```c
wl
water
waterValue
water_level_data
levelVal
```

表达同一个概念。

同一个概念只能有一种主要命名。

---

# 九、优先使用简单数据结构

如果只需要保存几个传感器数据，可以直接：

```c
typedef struct
{
    float temperature;
    float humidity;
    float water_level;
    float flow;
} SensorData_t;
```

避免为了小功能设计复杂的数据框架。

结构体字段必须使用完整、有意义的名字。

---

# 十、主程序必须非常容易看懂

推荐：

```c
int main(void)
{
    System_Init();

    Sensor_Init();
    Actuator_Init();
    Communication_Init();

    while (1)
    {
        Sensor_Update();

        Control_Update();

        Alarm_Update();

        Communication_Update();

        Display_Update();
    }
}
```

主循环必须能够让参赛者快速理解：

```text
采集
↓
判断
↓
控制
↓
报警
↓
通信
↓
显示
```

不要在 `main()` 里堆积几百行代码。

---

# 十一、业务逻辑优先使用 if / else / switch

比赛现场最容易修改的是：

```c
if
else
switch
case
```

因此优先使用这些结构。

例如：

```c
if (water_level < WATER_LOW_LEVEL)
{
    Pump_On();
}

if (water_level > WATER_HIGH_LEVEL)
{
    Pump_Off();
}
```

不要为了抽象而把简单判断隐藏在多层函数中。

---

# 十二、复杂业务使用状态机，但必须简单

推荐：

```c
typedef enum
{
    STATE_INIT,
    STATE_NORMAL,
    STATE_WARNING,
    STATE_ALARM,
    STATE_ERROR
} SystemState_t;
```

使用：

```c
switch (system_state)
{
    case STATE_NORMAL:
        Normal_Control();
        break;

    case STATE_ALARM:
        Alarm_Control();
        break;

    case STATE_ERROR:
        Error_Control();
        break;
}
```

状态数量不要无意义增加。

每一个状态都必须能够说明其用途。

---

# 十三、必须提供“调试日志”

重要变量和状态变化必须可以输出：

```text
[SENSOR]
[TEMP]
[FLOW]
[WATER]

[STATE]
[CONTROL]
[NETWORK]
[ERROR]
```

例如：

```c
DEBUG_PRINT("[SENSOR] water_level=%d\r\n", water_level);
DEBUG_PRINT("[CONTROL] pump=ON\r\n");
DEBUG_PRINT("[ERROR] flow sensor timeout\r\n");
```

调试日志必须能够回答：

1. 传感器有没有读到数据？
2. 数据是不是正常？
3. 控制逻辑有没有执行？
4. 执行器有没有动作？
5. 网络有没有连接？
6. 数据有没有发送？
7. 哪一步发生错误？

---

# 十四、必须提供DEBUG开关

例如：

```c
#define DEBUG_ENABLE 1
```

代码：

```c
#if DEBUG_ENABLE
    DEBUG_PRINT(...);
#endif
```

关闭DEBUG后不能影响正常功能。

---

# 十五、必须提供模块级测试程序

每一个重要模块都必须能够单独测试。

例如：

```text
test_temperature.c
test_water_level.c
test_pump.c
test_uart.c
test_network.c
```

至少提供：

```c
Sensor_Test();
Pump_Test();
Communication_Test();
```

用于现场排错。

---

# 十六、必须优先支持“单模块验证”

如果系统出问题，应该可以单独验证：

```text
传感器
↓
驱动
↓
执行器
↓
通信
↓
服务器
```

禁止所有功能强耦合到一起导致：

> 一个传感器坏掉，整个系统无法启动。

---

# 十七、初始化必须明确

所有模块初始化集中：

```c
System_Init();

Sensor_Init();
Actuator_Init();
Communication_Init();
Display_Init();
```

初始化函数内部不要隐藏大量业务逻辑。

---

# 十八、所有关键函数都必须有简短注释

注释回答三个问题：

```text
这个函数干什么？
输入是什么？
输出是什么？
```

例如：

```c
/**
 * 读取当前水位
 *
 * 返回值：
 * 0~100：水位百分比
 * -1：传感器异常
 */
int WaterLevel_Get(void);
```

不要写：

```c
// 获取数据
```

这种没有实际帮助的注释。

---

# 十九、关键业务代码必须添加“人类可读解释”

例如：

```c
/*
 * 水位控制逻辑：
 * 1. 水位低于下限 → 启动水泵
 * 2. 水位高于上限 → 停止水泵
 * 3. 中间区域 → 保持当前状态
 */
```

然后代码：

```c
if (water_level < WATER_LOW_LEVEL)
{
    Pump_On();
}
else if (water_level > WATER_HIGH_LEVEL)
{
    Pump_Off();
}
```

---

# 二十、不要把多个功能写成一个巨大函数

错误：

```c
void System_Control(void)
{
    // 500行代码
}
```

推荐：

```c
Sensor_Update();
Water_Control();
Temperature_Control();
Alarm_Update();
Communication_Update();
```

每个函数只负责一个主要功能。

---

# 二十一、但也不要过度拆函数

如果一个函数只有：

```c
a();
```

然后又创建一个：

```c
void Do_A()
{
    a();
}
```

这种没有意义的封装禁止。

原则：

> **一个函数完成一个明确功能。**

---

# 二十二、通信协议必须简单、可观察、容易修改

推荐设计统一数据包：

```text
HEADER
DEVICE_ID
COMMAND
DATA
CHECK
TAIL
```

同时提供：

```c
Send_Data();
Send_Command();
Parse_Message();
Process_Command();
```

所有协议字段必须有注释。

---

# 二十三、网络参数必须集中

例如：

```c
#define SERVER_IP       "192.168.1.100"
#define SERVER_PORT     8080
#define DEVICE_ID       "NODE_01"
```

禁止散落在多个文件里。

---

# 二十四、必须考虑网络断开

不能假设网络永远正常。

至少提供：

```text
连接
检测
重连
超时
错误状态
```

例如：

```c
if (!Network_IsConnected())
{
    Network_Reconnect();
}
```

---

# 二十五、错误处理必须简单明确

每个关键模块至少有：

```text
正常
异常
超时
恢复
```

例如：

```c
if (sensor_value < 0)
{
    sensor_error = 1;
}
```

不要让错误处理被隐藏在复杂框架里。

---

# 二十六、执行器默认必须安全

例如：

```text
系统刚启动
↓
水泵 OFF
电磁阀 CLOSED
电机 STOP
蜂鸣器 OFF
```

初始化时明确设置安全状态。

---

# 二十七、避免阻塞式代码

除非任务明确要求，否则尽量不要大量使用：

```c
Delay_ms(10000);
```

导致整个系统10秒不能工作。

优先使用：

```c
millis
timer
timestamp
```

实现非阻塞控制。

---

# 二十八、时间任务必须明确

推荐：

```c
if (Timer_Expired(&sensor_timer, SENSOR_PERIOD_MS))
{
    Sensor_Update();
}
```

而不是：

```c
Delay_ms(1000);
Sensor_Update();
```

这样现场修改采样周期会更简单。

---

# 二十九、代码必须适合“复制、替换、拼装”

所有常见功能必须设计成可以直接复制：

```c
/* 水位控制 */
if (water_level < WATER_LOW_LEVEL)
{
    Pump_On();
}
```

或者：

```c
/* 温度报警 */
if (temperature > TEMPERATURE_LIMIT)
{
    Alarm_On();
}
```

不要让一个简单功能依赖十几个隐藏条件。

---

# 三十、任何新增功能都优先以“最小修改”实现

当用户提出：

> “增加一个水位报警。”

优先方案：

1. 增加一个参数；
2. 增加一个判断；
3. 调用已有报警函数。

而不是重新设计整个控制系统。

---

# 三十一、现场修改优先级

所有代码必须尽量做到：

```text
第一优先：修改 config.h
第二优先：修改 application/control.c
第三优先：增加少量业务函数
最后才允许修改 drivers
```

现场如果任务变化，尽可能不要修改：

```text
BSP
HAL
底层驱动
通信底层
核心框架
```

---

# 三十二、生成代码时必须告诉我“改哪里”

每次修改代码时，除了给出代码，还必须告诉我：

```text
【本次修改】
文件：
application/control.c

修改位置：
Water_Control()

修改原因：
增加低水位启动条件
```

如果修改参数：

```text
【现场可修改】
config.h

WATER_LOW_LEVEL
WATER_HIGH_LEVEL
FLOW_LIMIT
```

---

# 三十三、禁止只给“完整代码”而不解释修改点

每次生成完整工程时，必须同时给：

```text
1. 项目目录
2. 每个文件作用
3. 关键函数列表
4. 现场最可能修改的位置
5. 调试方法
6. 常见错误
```

---

# 三十四、出现编译错误时优先保持原结构稳定

修复错误时：

> **优先最小修改。**

不要因为一个小错误重新重构整个项目。

例如：

错误：

```text
变量未定义
```

优先修复变量。

不要：

> “顺便重构整个通信架构。”

---

# 三十五、禁止未经说明地更换技术方案

例如当前使用：

```text
UART
```

不要突然改成：

```text
SPI
```

当前使用：

```text
TCP
```

不要突然改成：

```text
MQTT
```

除非明确说明原因并获得许可。

---

# 三十六、硬件接口必须清晰

必须提供：

```text
模块名称
MCU引脚
供电电压
通信方式
初始化函数
读取函数
控制函数
```

例如：

```text
水位传感器
VCC → 3.3V
GND → GND
OUT → PA3
接口 → ADC
读取 → WaterLevel_Get()
```

---

# 三十七、代码必须适应离线比赛

不要引入必须联网下载的依赖。

所有第三方依赖必须提前说明：

```text
名称
版本
用途
是否可以离线运行
```

优先使用已有SDK、标准库和项目已有依赖。

---

# 三十八、不要擅自使用不确定API

如果MCU型号、SDK版本、库版本不确定：

> 必须先根据当前工程确认，而不是凭经验猜API。

禁止为了让代码“看起来完整”而编造API。

---

# 三十九、生成代码前必须先检查现有项目

如果用户提供现有工程：

1. 先读取目录；
2. 找到已有驱动；
3. 找到已有初始化；
4. 找到已有main；
5. 找到已有通信协议；
6. 尽量复用现有接口。

禁止重复创建已有模块。

---

# 四十、最终验收标准

生成的代码必须同时满足：

### 可读

初学者能够理解核心流程。

### 可改

能快速找到：

```text
阈值
周期
IP
端口
设备ID
控制逻辑
```

### 可调

有：

```text
DEBUG
日志
模块测试
```

### 可复用

传感器和执行器能够独立调用。

### 可组合

多个模块可以快速拼成新功能。

### 可离线

不依赖互联网。

### 可比赛

能够支持：

> **在4小时内根据未知任务快速修改和组合。**

---

# 四十一、最终开发哲学

牢记：

> 不要为“程序员”写代码，要为“比赛现场的操作者”写代码。

现场人员需要看到的是：

```text
读什么？
怎么判断？
控制什么？
在哪里修改？
出了问题怎么看？
```

而不是：

```text
这个架构多么优雅。
```

最终目标：

> **让一个不会从零写代码、但能看懂基础 C 语言的参赛者，也可以在没有AI和互联网的情况下，通过阅读现有工程完成现场修改。**

任何设计如果增加了现场理解和修改难度，即使理论上更加“优雅”，也应该优先舍弃。