### 一、核心必备PCB模块（对应80分基础任务，必须实现）
以下模块是完成赛题基础硬件功能的最小集合，是PCB的核心分区，直接对应任务一至任务五的硬件要求。

#### 1. 主控最小系统模块
- **核心功能**：整个采集控制板的计算核心，负责传感器数据读取、数据预处理、继电器逻辑控制、网络通信协议处理。
- **电路组成**：MCU芯片（推荐STM32等主流嵌入式芯片）、晶振电路、复位电路、SWD/JTAG下载调试接口、启动配置电路。
- **对应赛题**：多源数据采集、执行器控制、网络传输的硬件载体。

#### 2. 多传感器采集与信号调理模块
针对4路传感器做适配电路，保障信号可被稳定采集：
- **2路温度传感器接口电路**：数字型传感器（如DS18B20）包含上拉电阻、滤波电容与接线端子；模拟型传感器（NTC）增加分压采样、RC滤波调理电路。
- **流量传感器脉冲处理电路**：针对霍尔式流量传感器的脉冲输出，增加施密特触发器整形、上拉滤波，对接MCU定时器/输入捕获通道实现计数。
- **压力传感器信号调理电路**：针对模拟输出型压力传感器（0-5V/4-20mA），包含I/V转换、运算放大、低通滤波，匹配MCU ADC输入量程；数字型压力传感器预留I2C/SPI通信接口。
- **对应赛题**：传感器部署与接线、多源数据同步采集、数据预处理的硬件支撑。

#### 3. 继电器驱动与执行器控制模块
- **核心功能**：实现弱电控制强电，完成水泵、加热模块的电源通断，同时实现强弱电电气隔离。
- **电路组成**：至少2路光耦隔离电路、三极管/MOS管驱动电路、继电器本体、续流二极管、输出接线端子；加热回路串联熔断器做过流保护。
- **设计要点**：PCB上强弱电分区布局，保留≥2mm爬电距离，避免干扰与安全隐患。
- **对应赛题**：执行器控制电路搭建、继电器控制水泵与加热模块。

#### 4. 电源管理模块
- **核心功能**：将输入电源转换为各模块所需电压，提供稳定、低干扰的供电。
- **电路组成**：电源输入接口+反接/过流/过压保护电路、一级降压（如12V转5V，给继电器、传感器供电）、二级降压（5V转3.3V，给主控、通信芯片供电）、模拟/数字电源分区滤波、电源指示电路。
- **设计要点**：模拟地与数字地单点共地，大电流回路与小信号回路分开布线。
- **对应赛题**：所有硬件模块的供电基础，保障系统稳定运行。

#### 5. 双通信接口模块
满足赛题“串口+网口”双通信通道要求：
- **串口通信电路**：USB转UART芯片（如CH340）+ USB接口，或预留TTL/RS232串口端子，用于参数调试、数据读取。
- **以太网通信电路**：以太网控制芯片（如W5500/ENC28J60）+ 网络变压器 + RJ45接口，包含芯片外围电路、状态指示灯；支持TCP/UDP/HTTP协议。
- **对应赛题**：通信链路搭建、基于TCP/UDP的数据传输、HTTP协议交互、智能判定服务对接。

#### 6. 状态指示与接线端子模块
- **状态指示**：电源灯、运行灯、通信指示灯、继电器状态灯，直观反馈系统工作状态。
- **接线端子**：所有传感器输入、执行器输出、电源输入均采用插拔式端子，规范接线，方便现场组装。
- **对应赛题**：接线规范要求，便于现场调试与故障排查。

---

### 二、可拓展PCB模块（对应20分进阶任务+性能优化）
这些模块对应赛题进阶拓展功能，或提升系统稳定性、精度与演示效果，可根据开发进度选做。

#### 1. 高精度温控驱动模块（对应「本地恒温闭环控制」8分）
- **功能**：替代纯开关式继电器，实现加热功率连续调节，提升PID温控精度，更容易达到±1℃稳态要求。
- **电路组成**：固态继电器（SSR）/大功率MOS管驱动电路、PWM隔离输出电路、温度硬件保护开关（串联加热回路，超温直接硬件断电）。
- **附加价值**：相比继电器启停，PWM调功温控更平稳，减少温度过冲。

#### 2. 硬件异常告警模块（对应「异常告警功能」5分）
- **功能**：实现硬件级异常触发与本地告警，补充软件告警的响应速度。
- **电路组成**：蜂鸣器驱动电路、高亮告警LED、硬件阈值比较器（针对过温、过压、欠流设置硬件阈值，超阈值直接触发告警）。
- **附加价值**：即使上位机/软件异常，硬件也能触发告警，提升系统可靠性。

#### 3. 无线通信拓展模块（对应「多终端自适应访问」3分）
- **功能**：补充有线以太网，实现无线接入，方便手机、平板等移动终端直接访问监控界面。
- **电路组成**：Wi-Fi模块（如ESP8266/ESP32-C3）+ 天线电路，与主控通过串口/SPI通信。
- **附加价值**：摆脱网线限制，移动端直接访问前端界面，天然适配多终端自适应需求。

#### 4. 本地离线存储模块（对应「数据统计分析」4分）
- **功能**：本地离线存储历史传感数据与告警日志，不依赖上位机数据库也能保存数据。
- **电路组成**：SPI Flash芯片（如W25Q系列）、SD卡卡槽电路，支持掉电数据不丢失。
- **附加价值**：本地可直接计算统计指标，减轻后端压力，断网也能保留数据。

#### 5. 安全保护拓展模块（规避扣分风险）
- **功能**：从硬件层面避免干烧、短路等危险操作，减少违规扣分。
- **电路组成**：
  - 水位检测接口：接入液位传感器，加热槽水位过低时硬件切断加热电源，防止干烧；
  - 水流检测接口：接入水流开关，水泵启动后无水流则触发保护；
  - 电源浪涌保护：输入侧增加TVS管、共模电感，提升抗干扰能力。
- **对应赛题**：避免加热模块干烧、严重漏水等危险操作的扣分。

#### 6. 本地人机交互模块（调试与展示加分）
- **功能**：脱离上位机也能本地查看数据、设置参数，现场演示更直观。
- **电路组成**：OLED/LCD显示屏接口、独立按键（设置阈值、手动控制）。
- **附加价值**：现场调试更高效，展示时可脱离电脑演示基础功能。

---

### PCB设计核心注意事项
1. **分区布局**：严格按照「强电区-弱电区」「模拟区-数字区」分区布线，减少电磁干扰；
2. **接地设计**：模拟地、数字地、功率地单点连接，避免地环路干扰；
3. **安规要求**：强电回路与弱电回路保留足够爬电距离，加热、水泵回路加粗走线；
4. **可测试性**：预留关键测试点，方便现场硬件排查与信号测量。
