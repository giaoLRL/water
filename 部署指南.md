# 智能水循环监测与温控物联网系统 · 部署指南

> 本指南面向「把系统在这台 Windows 机器上跑起来」的完整流程，包含首次部署、日常启停、
> 配置修改、故障排查与验收测试。
>
> **本文档中的命令都在本机（Windows + PowerShell 7）实际执行过**，每条都标注了实测状态：
>
> | 标记 | 含义 |
> | --- | --- |
> | ✅ | 已在本机实测通过，命令可直接复制使用 |
> | ⚠️ | 需要管理员权限或现场硬件条件，本机无法完整验证，已说明验证方式 |
>
> 实测时间：2026-09-18；实测环境：Windows + Python 3.12.9 + MySQL 8.0.46 + Edge。
> 代码版本：远端仓库 `839af1f`（2026-09-18 拉取更新，新增「报警联动个体化」与
> 「自定义传感器通道」），另有 2 处本地前端修复（控制开关卡支持布尔型自定义源、
> 指令下发后立即回读）与 1 次本地重构（**报警联动改「卡片即来源」：删除执行器档案与
> 通道声明，触发源与动作都从实时监控页的卡片里选**）；逐条记录见
> [附录 B](#12-附录-b本次实测记录)。

---

## 0. 三十秒速览

如果只是想把已经部署好的系统重新启动起来：

```powershell
cd "D:\water-main (1)\water-main\backend"
& "..\.venv\Scripts\python.exe" main.py
```

浏览器打开 <http://127.0.0.1:8000>，账号 `admin` / `admin123`。
这个窗口按 `Ctrl+C` 即停止服务。其他情况请看对应章节。

---

## 1. 部署目标与系统模型

| 项目 | 内容 |
| --- | --- |
| 前端入口 | <http://127.0.0.1:8000>（登录页即首页） |
| 接口文档 | <http://127.0.0.1:8000/docs> |
| 局域网入口 | `http://<本机 IP>:8000`，本机实测为 <http://192.168.31.170:8000> |
| 默认账号 | `admin` / `admin123`（首次启动自动创建，建议登录后修改） |
| 进程模型 | **只有一个后端进程**（FastAPI + Uvicorn），前端静态文件由后端一并托管，不需要单独启动前端 |
| 数据来源 | 现场 ESP32 `http://192.168.31.100`，后端 1Hz 轮询；设备离线时字段为 `null`（水位为 0）并提示离线 |
| 数据库 | 本机 MySQL 8 系统服务 `MySQL80`（3306），库 `iot_system`，账号 `iot_user` |

启动时后端会自动建表并执行表结构迁移（幂等，不丢历史数据），**不需要手工建表**。

---

## 2. 环境要求与实测环境

| 组件 | 要求 | 本机实测值 | 检查命令 |
| --- | --- | --- | --- |
| 操作系统 | Windows 10/11 | Windows | — |
| Python | 3.11+ | 3.12.9，`C:\Users\wxsw\AppData\Local\Programs\Python\Python312\python.exe` | `python --version` |
| MySQL | 8.x（系统服务） | 8.0.46，服务名 `MySQL80`，已设为开机自启 | `Get-Service MySQL80` |
| 浏览器 | Chrome / Edge | Edge `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` | — |
| Node.js | **仅前端 E2E 测试需要** | v24.19.0 | `node --version` |
| 现场设备 | ESP32 采集端 | ⚠️ 本次部署时设备未上电，处于离线降级状态 | `Test-NetConnection 192.168.31.100 -Port 80 -InformationLevel Quiet` |

> Python 依赖（FastAPI / Uvicorn / PyMySQL / DBUtils）**全部装在项目内的 `.venv` 里**，
> 不会污染系统 Python。删掉 `.venv` 目录即可完全撤销依赖安装。

---

## 3. 目录说明（重要）

本机存在两份项目副本，**以带 `(1)` 的为准**：

| 路径 | 说明 |
| --- | --- |
| `D:\water-main (1)\water-main` | ✅ **当前部署的项目**（最新版，含可编辑卡片网格仪表盘） |
| `D:\water-main\water-main` | ⚠️ 旧副本（旧版前端，无卡片网格）。已停止，未删除 |

> 端口 8000 只能被一个进程占用。如果两台副本同时启动，第二份会因端口冲突启动失败
> （见 [7.1](#71-启动报-errno-10048端口被占用)）。

关键路径：

```text
D:\water-main (1)\water-main\
├─ backend\              后端源码（main.py / config.py / api.py / custom_channels.py ...）
│  └─ tests\             接口测试与单元测试
├─ frontend\             前端静态页面（Vue 3 + ECharts + gridstack，本地库无 CDN）
├─ tests\e2e_web.js      前端端到端测试（真实浏览器 + CDP）
├─ .venv\                本项目专属虚拟环境（Python 依赖装在这里）
├─ .runtime\             运行日志（uvicorn-out.log / uvicorn-err.log）
├─ scripts\              启动脚本（内含旧项目遗留内容，本指南不依赖它们）
└─ 部署指南.md           本文档
```

---

## 4. 首次部署（从零到可访问）

### 4.1 检查前置条件 ✅

```powershell
python --version
Get-Service MySQL80 | Select-Object Name, Status
```

实测输出：

```text
Python 3.12.9
Name     Status
MySQL80  Running
```

`MySQL80` 必须是 `Running`；不是的话见 [7.3](#73-启动报-2003-cant-connect-to-mysql-server数据库没启动)。

### 4.2 创建虚拟环境 ✅

```powershell
cd "D:\water-main (1)\water-main"
python -m venv .venv
& ".\.venv\Scripts\python.exe" -V
```

实测输出：`Python 3.12.9`。虚拟环境路径为 `D:\water-main (1)\water-main\.venv`。

### 4.3 安装依赖 ✅

```powershell
cd "D:\water-main (1)\water-main"
& ".\.venv\Scripts\python.exe" -m pip install --upgrade pip
& ".\.venv\Scripts\python.exe" -m pip install -r backend\requirements.txt
```

实测结果：`Successfully installed ... fastapi-0.141.1 uvicorn-0.53.0 pymysql-1.2.3 DBUtils-3.2.0 ...`

校验依赖确实装在 `.venv` 里：

```powershell
& ".\.venv\Scripts\python.exe" -c "import sys, fastapi; print(sys.prefix); print(fastapi.__file__)"
```

实测输出（路径指向 `.venv` 即为正确）：

```text
D:\water-main (1)\water-main\.venv
D:\water-main (1)\water-main\.venv\Lib\site-packages\fastapi\__init__.py
```

### 4.4 准备数据库

分两种情况，先确认属于哪一种：

```powershell
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql -h 127.0.0.1 -u iot_user -piot_pass_2026 -D iot_system -e "select 1;"
```

| 结果 | 含义 | 需要做的事 |
| --- | --- | --- |
| 输出 `1`，无报错 | ✅ 库和账号都已就绪（**本机当前就是这种情况**） | 跳到 [4.5](#45-启动后端) |
| `ERROR 1045 (28000): Access denied ...` | ⚠️ 账号或密码不对 | 见 [7.4](#74-启动报-1045-access-denied数据库账号密码不对) |
| `ERROR 1049 (42000): Unknown database 'iot_system'` | ⚠️ 库还没建 | 用下面的 SQL 建库 |

**新建库与账号**（需要 MySQL 的 `root` 账号密码；本机 `root` 有密码，因此以下语句
**未在本机执行**，仅做了语法校验：执行时返回的是权限错误 `ERROR 1227 / ERROR 1044`
而不是语法错误 `ERROR 1064`，说明语句本身合法）⚠️：

```sql
CREATE DATABASE IF NOT EXISTS iot_system DEFAULT CHARACTER SET utf8mb4;
CREATE USER IF NOT EXISTS 'iot_user'@'127.0.0.1' IDENTIFIED BY 'iot_pass_2026';
CREATE USER IF NOT EXISTS 'iot_user'@'localhost' IDENTIFIED BY 'iot_pass_2026';
GRANT ALL ON iot_system.* TO 'iot_user'@'127.0.0.1';
GRANT ALL ON iot_system.* TO 'iot_user'@'localhost';
FLUSH PRIVILEGES;
```

执行方式（把 `<root密码>` 换成实际密码，会提示输入密码）：

```powershell
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql -h 127.0.0.1 -u root -p -e "CREATE DATABASE IF NOT EXISTS iot_system DEFAULT CHARACTER SET utf8mb4;"
```

> 若 `root` 也登录不了（实测本机 `mysql -u root --password=` 返回
> `ERROR 1045 (28000): Access denied for user 'root'@'localhost' (using password: NO)`），
> 说明 root 设了密码 —— 用 MySQL Installer 重设，或让管理员提供凭据。
> **建表不需要执行任何 SQL**：后端启动时自动创建 `water_sensors` / `alarms` /
> `custom_sensor_data` / `control_log` / `config` / `users` 并做幂等迁移
> （`custom_sensor_data` 为 2026-09-18 更新新增，用于自定义传感器通道的历史与统计）。

### 4.5 启动后端

**方式一：前台启动（推荐，能实时看到采集日志）** ✅

```powershell
cd "D:\water-main (1)\water-main\backend"
& "..\.venv\Scripts\python.exe" main.py
```

实测启动日志（设备离线时）：

```text
INFO:     Started server process [24280]
INFO:     Waiting for application startup.
2026-09-18 07:10:48,294 [INFO] water: 采集设备: http://192.168.31.100 (超时1.5s) 支持通道: flow,volume,pump,pump_target,volume_reset,level_heater,temperature,pressure,heater,light
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
2026-09-18 07:10:49,820 [INFO] water: [SENSOR] flow=-- L/min total=-- L pump=unknown target=0.00 online=False ... err=无法连接设备 http://192.168.31.100：<urlopen error timed out>
```

按 `Ctrl+C` 停止，实测输出：

```text
INFO:     Shutting down
INFO:     Waiting for application shutdown.
INFO:     Application shutdown complete.
INFO:     Finished server process [24280]
```

**方式二：后台启动（关掉 PowerShell 窗口也继续跑，日志写文件）** ✅

```powershell
cd "D:\water-main (1)\water-main\backend"
$env:PYTHONUTF8 = "1"
Start-Process -FilePath "D:\water-main (1)\water-main\.venv\Scripts\python.exe" `
  -ArgumentList @("-m","uvicorn","main:app","--host","0.0.0.0","--port","8000") `
  -WorkingDirectory (Get-Location).Path -WindowStyle Hidden `
  -RedirectStandardOutput "D:\water-main (1)\water-main\.runtime\uvicorn-out.log" `
  -RedirectStandardError  "D:\water-main (1)\water-main\.runtime\uvicorn-err.log"
```

实测：命令执行后 1~5 秒内 8000 端口进入监听。

> `$env:PYTHONUTF8 = "1"` 的作用是让日志按 UTF-8 写入，方便直接用
> `Get-Content -Encoding UTF8` 阅读中文；不加也能跑，但日志文件是 GBK 编码
> （见 [7.9](#79-日志文件里中文是乱码)）。
>
> 路径里有空格和括号，`-ArgumentList` 里**不要**传 `--app-dir` 这类含路径的参数
> （实测会被空格拆断：`Error: Got unexpected extra argument ((1)\water-main\backend)`），
> 用 `-WorkingDirectory` 指定工作目录即可。

**方式三：从项目根目录启动** ✅（等价可用，适合在根目录按习惯敲命令）

```powershell
cd "D:\water-main (1)\water-main"
& ".\.venv\Scripts\python.exe" backend\main.py
```

实测：同样能监听 8000 并正常托管前端（前端目录按脚本绝对路径定位，与当前目录无关）。

### 4.6 验收 ✅

把下面这段整体粘贴执行，即为「一键体检」：

```powershell
$base = "http://127.0.0.1:8000"
"前端页面: " + (Invoke-WebRequest "$base/" -TimeoutSec 5).StatusCode
"接口文档: " + (Invoke-WebRequest "$base/docs" -TimeoutSec 5).StatusCode
$login = Invoke-RestMethod "$base/api/auth/login" -Method Post -ContentType "application/json" `
  -Body (@{username="admin";password="admin123"} | ConvertTo-Json)
$h = @{ Authorization = "Bearer " + $login.data.token }
$sys = (Invoke-RestMethod "$base/api/water/system" -Headers $h).data
"数据库: $($sys.database_ok) | 设备在线: $($sys.device_online) | 采集周期: $($sys.period)s | 累计运行: $([math]::Round($sys.uptime_s/60,1)) 分钟"
```

实测输出：

```text
前端页面: 200
接口文档: 200
数据库: True | 设备在线: False | 采集周期: 1s | 累计运行: 3.4 分钟
```

判定标准：

- 两个 `200` → 后端与前端托管正常；
- `数据库: True` → MySQL 连接池正常；
- `设备在线` 为 `False` 且页面显示「离线」→ 属于正常降级（设备没上电），不是部署故障；
- 打开 <http://127.0.0.1:8000> 能看到登录页并成功登录，即部署完成。

---

## 5. 日常启停与运维

### 5.1 判断服务是否在运行 ✅

```powershell
Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalAddress, LocalPort, OwningProcess
```

- 有输出 → 服务已在运行（`OwningProcess` 即进程号）；
- 无输出 → 服务未运行。

### 5.2 启动

见 [4.5](#45-启动后端)（前台 / 后台 / 根目录三种方式均已实测）。

### 5.3 停止 ✅

**前台窗口**：直接按 `Ctrl+C`。

**后台进程**（一行命令，实测有效）：

```powershell
Stop-Process -Id (Get-NetTCPConnection -LocalPort 8000 -State Listen).OwningProcess -Force
```

实测结果：命令返回后端口立即释放，且不会有残留 `python.exe` 进程。

> 如果命令报找不到对象，说明服务本来就没在运行。
> 想按名字清理所有残留 Python 进程：`Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force`
> （⚠️ 会一并结束本机其他 Python 脚本，确认没有别的脚本在跑再用）。

### 5.4 重启 ✅

```powershell
Stop-Process -Id (Get-NetTCPConnection -LocalPort 8000 -State Listen).OwningProcess -Force
Start-Sleep -Seconds 3
cd "D:\water-main (1)\water-main\backend"
& "..\.venv\Scripts\python.exe" main.py
```

实测：停止后 3 秒内端口释放，重新启动后正常监听，历史数据与仪表盘布局保持不变。

### 5.5 查看日志 ✅

| 文件 | 内容 |
| --- | --- |
| `.runtime\uvicorn-out.log` | HTTP 访问日志（谁请求了哪个接口、状态码） |
| `.runtime\uvicorn-err.log` | 采集/控制/告警等业务日志（每秒一条 `[SENSOR]`） |

```powershell
Get-Content "D:\water-main (1)\water-main\.runtime\uvicorn-err.log" -Encoding UTF8 -Tail 20
```

前台窗口运行的实例不写文件，日志直接打在窗口里。

### 5.6 更新到最新版本（拉取远端仓库）✅

远端仓库：<https://github.com/giaoLRL/water>。本机项目目录**已经初始化为 git 仓库**
（`git init` + 关联 `origin`，HEAD 与远端 `main` 对齐），所以更新直接用 `git pull`：

```powershell
cd "D:\water-main (1)\water-main"
git status                                        # 先看有没有未提交改动
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 pull origin main
```

提交/推送同理（本机必须带代理参数）：

```powershell
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 add -A
git commit -m "改动说明"
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main
```

> `.venv/`、`.runtime/`、`__pycache__/`、`*.log` 都在 `.gitignore` 里，不会被提交。

**若哪天又拿到一份压缩包副本（目录不是 git 仓库）**，用「克隆到临时目录 → 同步覆盖」的备用流程：

```powershell
# 1) 先备份本地文件（建议，出错可回滚）
$stamp = Get-Date -Format "yyyyMMdd-HHmmss"
robocopy "D:\water-main (1)\water-main" "$env:TEMP\water-local-backup-$stamp" /E /XD ".venv" ".runtime" "__pycache__"

# 2) 克隆远端到临时目录（本机必须走代理，见下面的说明）
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 `
    clone --depth 50 https://github.com/giaoLRL/water.git "$env:TEMP\water-repo"

# 3) 同步覆盖到项目目录（不加 /MIR，避免删掉本地 .venv / .runtime / 部署指南.md）
robocopy "$env:TEMP\water-repo" "D:\water-main (1)\water-main" /E /XD ".git"

# 4) 重启后端（旧进程里还是旧代码，必须重启才生效，见 5.4）
```

实测（2026-09-18）：

- 不走代理直接克隆会失败：`fatal: unable to access 'https://github.com/giaoLRL/water.git/': Recv failure: Connection was reset`；
  本机 Clash 的混合端口 `127.0.0.1:7897` 可用（`Test-NetConnection 127.0.0.1 -Port 7897` 为 True），加上 `-c http.proxy=...` 后 2 秒完成克隆；
- `robocopy` 退出码 **3 表示成功**（已复制文件 + 目标存在额外文件），不是错误；
- 同步后逐文件校验：43 个文件与远端 `839af1f` 完全一致，本地独有的 `.venv`、`.runtime`、
  `部署指南.md` 均保留；
- 更新后新表 `custom_sensor_data` 由后端自动创建，三条测试全绿（见 [8.2](#82-本机实测结果2026-09-18)）。

注意事项：

- 同步是「以远端为准」，**本地对仓库内文件的改动会被覆盖**（例如 README 里的指南入口，
  更新后需要重新加回）；
- 覆盖前务必确认后端已停止或稍后重启，否则运行中的进程仍执行旧代码；
- 想以后用增量 `git pull`：可在项目目录 `git init` + `git remote add origin ...`，
  但要注意 `.venv` / `.runtime` 需保持在忽略列表里。

---

## 6. 配置修改

### 6.1 环境变量覆盖（无需改代码） ✅

实测：设置变量后启动，配置立即生效（含代理白名单自动加入设备主机）：

```powershell
$env:WATER_DEVICE_URL   = "http://192.168.31.101"   # 换现场设备地址
$env:WATER_SAMPLE_PERIOD = "2.5"                    # 采集周期(秒)，须 ≥1Hz 场景下取 1
```

实测输出：

```text
DEVICE_URL = http://192.168.31.101
PERIOD    = 2.5
PROXY_HOSTS = ['127.0.0.1', '192.168.31.101', 'localhost']
```

> 环境变量只对**当前这个 PowerShell 窗口**及其启动的进程有效，关掉窗口即失效；
> 要长期生效就写进 `backend\config.py`。

常用变量：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `WATER_DEVICE_URL` | `http://192.168.31.100` | 现场采集控制端地址 |
| `WATER_DEVICE_TIMEOUT` | `1.5` | 单次设备请求超时（秒） |
| `WATER_DEVICE_OFFLINE_AFTER` | `5.0` | 连续无响应多久判定离线 |
| `WATER_SAMPLE_PERIOD` | `1.0` | 采集周期（秒） |
| `IOT_DB_HOST/PORT/USER/PASSWORD/NAME` | 127.0.0.1 / 3306 / iot_user / iot_pass_2026 / iot_system | 数据库连接 |
| `IOT_JWT_SECRET` | `iot-water-secret` | 令牌签名密钥（生产环境建议改） |
| `WATER_PID_ENABLED` | `0` | 恒温闭环默认开关 |
| `WATER_JUDGE_URL` | 空 | 判定服务地址，留空则不上报 |
| `WATER_DASHBOARD_PROXY_HOSTS` | 空 | 自定义卡片接口代理白名单（逗号分隔追加） |

### 6.2 改代码里的现场参数 ✅

现场设备地址、通道能力、告警阈值、PID 参数、判定服务地址都集中在
`backend\config.py`（均带【现场修改】注释）。改完**重启后端**生效。

### 6.3 通过界面修改（改完即生效，无需重启）

登录后进入「系统配置」页可改：告警阈值、采集周期、双水槽容积、账号与角色权限矩阵、
**报警联动规则**（触发源与动作都直接选实时监控页上的卡片，内置卡和自建卡都能选）；
「实时监控」页可进入编辑模式自由增删/拖拽卡片。这些操作都会写入操作日志（含操作人）。

新增自定义传感器＝在实时监控页加一张「自定义接口」卡（URL+取值路径+轮询周期），
后端自动按卡片轮询入库，历史曲线/数据统计/报警联动都会自动跟上——**不需要在配置页重复声明**。
恢复默认布局：接口 `POST /api/water/dashboard/layout/reset`（需 `cfg_system` 权限），
或界面上的「恢复默认」按钮。

> ⚠️ 不要把水槽容积填成 0，否则水位卡的水量估算会显示 0。
> ⚠️ 自定义接口卡的「取值路径」填的是 **JSON 字段名**（如 `pump2`、`data.temperature`），
> 不是 URL 路径；填成 `/api/pump2/state` 会取不到值。配置对话框里的「预览」会提示
> 「该路径未取到值，请检查路径写法」。
> ⚠️ 「清零累计水量」是不可恢复操作，后端有二次确认与权限校验，非必要不要点。

---

## 7. 故障排查手册

每条都是「症状 → 原因 → 处理」，报错原文为实测抓取。

### 7.1 启动报 Errno 10048：端口被占用

**症状**（实测原文，日志为 GBK 时中文可能显示为乱码）：

```text
ERROR:    [Errno 10048] error while attempting to bind on address ('0.0.0.0', 8000):
          [winerror 10048] 通常每个套接字地址(协议/网络地址/端口)只允许使用一次。
```

**原因**：8000 已被别的进程占用（多半是另一个后端实例）。

**处理**：

```powershell
# 1) 看谁占着
Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object OwningProcess
Get-CimInstance Win32_Process -Filter "ProcessId=<上面的进程号>" | Select-Object CommandLine

# 2) 如果确实是旧的后端实例，结束它
Stop-Process -Id (Get-NetTCPConnection -LocalPort 8000 -State Listen).OwningProcess -Force
```

若占用者是别的软件、不能结束，就换端口启动（见 [7.11](#711-想换端口启动)）。

### 7.2 启动报「不是可识别的 cmdlet / 不是可执行程序」

**症状**（实测原文）：

```text
&: The term '..\.venv\Scripts\python.exe' is not recognized as a name of a cmdlet,
function, script file, or executable program.
```

**原因**：虚拟环境不存在或路径写错（例如 `.venv` 被删掉、或当前目录不是 `backend`）。

**处理**：

```powershell
cd "D:\water-main (1)\water-main"
Test-Path ".\.venv\Scripts\python.exe"    # False 说明虚拟环境丢了
python -m venv .venv                      # 重建
& ".\.venv\Scripts\python.exe" -m pip install -r backend\requirements.txt
```

### 7.3 启动报 2003 Can't connect to MySQL server：数据库没启动

**症状**（实测原文）：

```text
pymysql.err.OperationalError: (2003, "Can't connect to MySQL server on '127.0.0.1'
  ([WinError 10061] 由于目标计算机积极拒绝，无法连接。)")
ERROR:    Application startup failed. Exiting.
```

**原因**：MySQL 服务没跑，或端口/主机配置不对。⚠️ 本机测试用的是把 `IOT_DB_PORT`
指向不存在的端口来复现，效果与「服务停止」一致（同样报 2003 / 10061）。

**处理**：

```powershell
Get-Service MySQL80 | Select-Object Name, Status          # 看状态
Start-Service MySQL80                                     # ⚠️ 需管理员权限
```

非管理员时用管理员身份打开 PowerShell 再执行，或在「服务」管理器里启动 `MySQL80`。
启动后重跑一键体检（[4.6](#46-验收)），`数据库` 应为 `True`。

### 7.4 启动报 1045 Access denied：数据库账号密码不对

**症状**（实测原文）：

```text
pymysql.err.OperationalError: (1045, "Access denied for user 'iot_user'@'localhost' (using password: YES)")
```

**原因**：`iot_user` 密码不对，或该账号没有 `iot_system` 权限。

**处理**：先确认凭据，再决定改配置还是改数据库。

```powershell
# 用配置里的凭据手工连一次
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql -h 127.0.0.1 -u iot_user -piot_pass_2026 -D iot_system -e "select 1;"
```

- 手工能连、后端连不上 → 后端读到了别的环境变量，检查是否有残留的 `IOT_DB_*`（重开窗口即可清除），或 `config.py` 被改过；
- 手工也连不上 → 用 MySQL 管理员账号重设密码/授权（见 [4.4](#44-准备数据库) 的 SQL）。

### 7.5 建库语句报 1227 / 1044：权限不足

**症状**（实测原文）：

```text
ERROR 1227 (42000) at line 1: Access denied; you need (at least one of) the CREATE USER privilege(s) for this operation
ERROR 1044 (42000) at line 1: Access denied for user 'iot_user'@'127.0.0.1' to database 'iot_system'
ERROR 1227 (42000) at line 1: Access denied; you need (at least one of) the RELOAD privilege(s) for this operation
```

**原因**：建库建号必须用 `root`（或等价管理员）账号，`iot_user` 只有 `iot_system` 库的权限。
实测 `iot_system` 库已存在时，`CREATE DATABASE IF NOT EXISTS iot_system` 用 `iot_user`
执行不会报错（MySQL 跳过已存在对象的建库检查），但 `CREATE USER` / `GRANT` / `FLUSH` 一定需要 root。

**处理**：用 `root` 执行 [4.4](#44-准备数据库) 的 SQL。

### 7.6 页面能打开，但数据全是 `--`、显示「离线」

**症状**：页面正常、能登录，但流量/温度/压力显示 `--`，水槽标注「离线」。

**原因**：现场 ESP32 没有响应（未上电、未连 WiFi、IP 变了）。实测本机当前就是这种状态：

```text
err=无法连接设备 http://192.168.31.100：<urlopen error timed out>
```

**处理**（按顺序排查）：

```powershell
# 1) 本机能不能通到设备
Test-NetConnection 192.168.31.100 -Port 80 -InformationLevel Quiet   # 期望 True
Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -notlike "127.*" }

# 2) 能通就看设备真实接口
(Invoke-WebRequest "http://192.168.31.100/api/data" -TimeoutSec 3).Content
```

- 不通：检查设备供电、WiFi、IP 是否仍是 `192.168.31.100`；同网段才能直连
  （本机实测 WLAN 地址 `192.168.31.170`，与设备同网段）；
- IP 变了：改 `WATER_DEVICE_URL` 或 `config.py`，重启后端（见 [6.1](#61-环境变量覆盖无需改代码)）；
- 设备在线但个别通道为空：属正常能力裁剪（例如储水槽没装液位传感器，
  界面标「无传感器」，水位恒为 0，不会造假数据）。

设备恢复后**无需重启后端**，下一个采集周期（1 秒内）即自动恢复显示。

### 7.7 手机/其他电脑打不开 `http://192.168.31.170:8000`

**原因**：后端监听 `0.0.0.0` 没问题，但 Windows 防火墙默认拦截入站 8000。
实测本机防火墙三个配置（域/专用/公用）均为开启，且没有 8000 端口的入站规则。⚠️ 需管理员。

**处理**（管理员 PowerShell，执行一次即可）：

```powershell
New-NetFirewallRule -DisplayName "Water IoT 8000" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow -Profile Private
```

校验规则已存在：

```powershell
Get-NetFirewallRule -DisplayName "Water IoT 8000" | Select-Object DisplayName, Enabled, Direction, Action
```

其他自查点：手机与电脑在同一网段；电脑没开 VPN/代理；IP 没变（`Get-NetIPAddress` 确认）。

### 7.8 登录不上 / 忘记管理员密码

**处理顺序**：

1. 先确认账号密码：默认 `admin` / `admin123`（首启自动创建）；
2. 确认后端在跑（[5.1](#51-判断服务是否在运行)）；
3. 忘记密码时用下面的 SQL 直接改密码（**实测流程已跑通并还原**）：

```powershell
# 第 1 步：生成新密码的哈希（把 NewPass2026 换成你要的密码）
$new = & "D:\water-main (1)\water-main\.venv\Scripts\python.exe" -c "import sys; sys.path.insert(0, r'D:\water-main (1)\water-main\backend'); import auth; print(auth.hash_password('NewPass2026'))"

# 第 2 步：写回数据库（无需重启后端，立即生效）
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql -h 127.0.0.1 -u iot_user -piot_pass_2026 -D iot_system -e "update users set password_hash='$new' where username='admin';"

# 第 3 步：用新密码登录验证
Invoke-RestMethod "http://127.0.0.1:8000/api/auth/login" -Method Post -ContentType "application/json" -Body (@{username="admin";password="NewPass2026"} | ConvertTo-Json)
```

实测输出 `code=0`（登录成功）；恢复原密码后 `admin123` 同样返回 `code=0`。
注意密码哈希里含 `$`，**必须用单引号包 SQL**（PowerShell 双引号会吃掉 `$`）。

> 另一种兜底：把 `users` 表清空后重启后端，会自动重建 `admin` / `admin123`
> （`ensure_admin` 只在用户数为 0 时创建）——但这会删掉所有账号，谨慎使用。

### 7.9 日志文件里中文是乱码

**原因**：不加 `PYTHONUTF8=1` 时，Python 重定向输出使用系统 GBK 编码；
用 UTF-8 去读就成乱码。实测两种读法：

```powershell
# 正确的两种做法，二选一
Get-Content ".runtime\uvicorn-err.log" -Encoding ([System.Text.Encoding]::GetEncoding(936)) -Tail 5   # 按 GBK 读
Get-Content ".runtime\uvicorn-err.log" -Encoding UTF8 -Tail 5                                          # 按 UTF-8 读（需启动时设了 PYTHONUTF8=1）
```

永久解决：启动前 `$env:PYTHONUTF8 = "1"`（见 [4.5](#45-启动后端) 方式二）。
前台窗口运行不受影响，窗口里中文正常显示。

### 7.10 页面还是旧界面 / 白屏

**处理**：

1. 强刷：`Ctrl+F5`（后端已对静态文件加了 `no-store`，但仍建议强刷）；
2. 确认访问的是**当前副本**：旧副本 `D:\water-main\water-main` 的前端是旧版
   （引 `waterLoop.js`，没有卡片网格）。判断方法：

```powershell
(Invoke-WebRequest "http://127.0.0.1:8000/" -TimeoutSec 5).Content | Select-String "widgets.js"
```

有 `widgets.js` 说明是当前版本；只有 `waterLoop.js` 说明 8000 上跑的是旧副本。

3. 白屏时看浏览器控制台（F12）；也可用无头浏览器抓一次 DOM 验证服务端渲染：

```powershell
& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --no-first-run `
  --user-data-dir="$env:TEMP\e2e-diag" --virtual-time-budget=6000 --dump-dom "http://127.0.0.1:8000/" |
  Select-String -SimpleMatch 'login-card' | Select-Object -First 1
```

实测能抓到 `<div class="login-card">...`，说明服务端与前端都正常（白屏就是浏览器缓存或扩展问题）。

### 7.11 想换端口启动

前端按相对路径加载资源，换端口不影响功能：

```powershell
cd "D:\water-main (1)\water-main\backend"
& "..\.venv\Scripts\python.exe" -m uvicorn main:app --host 0.0.0.0 --port 8080
```

访问 <http://127.0.0.1:8080>；E2E 测试用 `$env:E2E_BASE = "http://127.0.0.1:8080"` 指向新端口。

### 7.12 前端 E2E 测试失败（登录页渲染失败 / Illegal invocation）

**症状**（实测原文）：

```text
  [FAIL] 登录页渲染(#app 有内容)
E2E 运行失败: Error: 页面执行异常: TypeError: Illegal invocation
```

**原因**：测试脚本用 9222 调试端口驱动浏览器；上一次测试异常中断后残留的
无头浏览器进程会被下一次运行误连，导致所有页面断言失败。

**处理**（实测有效）：

```powershell
# 1) 清理残留的无头浏览器与调试端口
Get-CimInstance Win32_Process -Filter "Name='msedge.exe'" |
  Where-Object { $_.CommandLine -like "*headless*" -or $_.CommandLine -like "*9222*" } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }

# 2) 确认端口空闲后再重跑
node tests\e2e_web.js
```

另外**不要并发跑测试**：接口测试与 E2E 会同时改配置、抢浏览器端口，
实测并发时 E2E 必然失败。请按 单元测试 → 接口测试 → E2E 的顺序串行执行。

### 7.13 数据库在离线期间持续变大

**现象**：设备离线时，`water_sensors` 仍每秒新增一行（字段全为 `NULL`）。

**原因**：这是当前设计——采集循环不区分设备是否在线，按 1Hz 入库并写 `NULL`
（遵循「无数据不伪造 0 值，统一为 NULL」的约束）。

**处理**：属预期行为，无需处理；长期离线演示前若想控制体积，可先把采集周期调大
（系统配置页或 `WATER_SAMPLE_PERIOD`），⚠️ 删历史数据属破坏性操作，请自行确认后
再执行 SQL。

---

## 8. 测试与验收

### 8.1 三条测试命令（**串行执行**）✅

前提：后端已启动、MySQL 可用。

```powershell
cd "D:\water-main (1)\water-main"

# 1) 液位通道契约单元测试（不访问网络/数据库）
& ".\.venv\Scripts\python.exe" backend\tests\test_level.py

# 2) 后端接口功能测试（需后端已启动）
& ".\.venv\Scripts\python.exe" backend\tests\test_api.py

# 3) 前端端到端测试（真实浏览器 + CDP，需后端已启动）
node tests\e2e_web.js
```

### 8.2 本机实测结果（2026-09-18）✅

| 测试 | 结果 |
| --- | --- |
| `backend\tests\test_level.py` | **全部通过**（无模拟数据、无数据为 0、离线归 0 等契约） |
| `backend\tests\test_api.py` | **146 通过 / 0 失败 / 4 提示** |
| `tests\e2e_web.js` | **151 通过 / 0 失败 / 0 提示**，无前端 JS 报错 |

> 上表为 2026-09-18 最后一次实测结果（含本地前端修复与「卡片即来源」重构），现场设备在线；
> 设备离线时若干与设备状态相关的校验会降级为提示（WARN）而非失败。

> 提示（WARN）来自设备离线：与设备在线状态相关的断言会降级为提示而非失败，
> 属于预期行为；设备上线后可复跑确认全绿。

### 8.3 测试安全约定

- 三条测试**都不会启动水泵、不会清零累计水量**；
- E2E 点「清零累计水量」只验证二次确认弹窗出现，然后选择「取消」；
- 接口测试默认只执行安全动作（关水泵、读状态、把定量目标设为 0）；
- 需要完整验证开泵链路时才用 `$env:WATER_TEST_ACTUATE = "1"`（会真实开泵抽水，慎用）。

### 8.4 交付前自查清单（对应 AGENTS.md 第 7 节）

- [x] `python main.py` 启动无报错，`/docs` 可访问（实测 200）
- [x] 启动日志打印采集设备地址、支持通道、PID 状态
- [x] `/api/water/realtime` 返回字段齐全并带 `features` 能力表（实测 code=0）
- [x] 传感数据周期入库，`/api/water/history` 可查，无数据通道为 `NULL`
- [x] 水泵控制失败返回 40003（离线时），不伪造成功
- [x] 实时监控页为全卡片网格，可增删/拖拽/缩放（E2E 覆盖）
- [x] 自定义卡片接口代理拒绝白名单外地址（接口测试覆盖）
- [x] 储水槽标注「无传感器」、离线标注「离线」，水位与后端一致
- [x] 操作日志记录操作人与来源，不含密码明文（接口测试覆盖）
- [x] 报警联动可在系统配置页编辑，**触发源与动作都从实时监控页的卡片里选**（内置卡/自建卡皆可），保存即生效并写 `config.link` 日志（接口测试与 E2E 覆盖）
- [x] 自定义传感器通道由「自定义接口」卡声明：realtime 快照含读数、历史/统计出现对应标签、联动触发源可选，删卡即停止采集（接口测试与 E2E 覆盖）
- [x] 系统配置页无「执行器档案/自定义传感器通道」区块，后端无对应声明端点（接口测试覆盖）
- [x] 三条测试全部通过
- [ ] 现场设备在线时的实时读数与开泵链路（⚠️ 需设备上电后现场确认）

---

## 9. 开机自启（可选）

### 9.1 方案 A：启动文件夹（无需管理员，已实测）✅

1. 按 `Win+R` 输入 `shell:startup` 回车，打开启动文件夹
   （本机为 `C:\Users\wxsw\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup`）；
2. 新建文件 `water-iot-backend.cmd`，内容如下（**注释请用英文**，实测中文注释在
   cmd 的 GBK 环境下会乱码报错）：

```bat
@echo off
rem Water IoT backend autostart (delete this file to disable autostart)
cd /d "D:\water-main (1)\water-main\backend"
set PYTHONUTF8=1
start "WaterIoT Backend" /min "D:\water-main (1)\water-main\.venv\Scripts\python.exe" -m uvicorn main:app --host 0.0.0.0 --port 8000
```

3. 双击（或执行 `cmd /c "…\water-iot-backend.cmd"`）验证：实测 6 秒内 8000 端口进入监听、
   `/docs` 返回 200，任务栏出现最小化的「WaterIoT Backend」窗口（关掉窗口即停止服务）。

**取消自启**：删掉该 `.cmd` 文件即可（实测删除后不再自启）。

### 9.2 方案 B：计划任务（需管理员）⚠️

实测以普通权限执行 `Register-ScheduledTask` 会被拒绝：

```text
Register-ScheduledTask: 拒绝访问。
```

需要「以管理员身份运行」的 PowerShell 执行：

```powershell
$repo = "D:\water-main (1)\water-main"
$action  = New-ScheduledTaskAction -Execute "$repo\.venv\Scripts\python.exe" `
  -Argument "-m uvicorn main:app --host 0.0.0.0 --port 8000" -WorkingDirectory "$repo\backend"
$trigger = New-ScheduledTaskTrigger -AtLogOn
Register-ScheduledTask -TaskName "WaterIoTBackend" -Action $action -Trigger $trigger -Force
```

验证与卸载：

```powershell
Start-ScheduledTask -TaskName "WaterIoTBackend"      # 立即试跑一次
Get-ScheduledTask -TaskName "WaterIoTBackend" | Select-Object TaskName, State
Unregister-ScheduledTask -TaskName "WaterIoTBackend" -Confirm:$false   # 取消自启
```

> 本机当前**没有**创建任何自启项（实测 `Get-ScheduledTask -TaskName WaterIoTBackend`
> 无结果，启动文件夹也没有残留文件），系统处于「手动启动」状态。

---

## 10. 回退与清理

> 本节命令为**撤场时使用**，其中删除类命令本次**未在本机执行**（避免破坏现有部署），
> 执行前请自行确认路径与后果。

```powershell
# 1) 停止服务
Stop-Process -Id (Get-NetTCPConnection -LocalPort 8000 -State Listen).OwningProcess -Force

# 2) 只撤销 Python 环境（保留代码与数据）
#    删除虚拟环境目录后，依赖即完全移除
Remove-Item -LiteralPath "D:\water-main (1)\water-main\.venv" -Recurse -Force

# 3) 清运行日志（可选）
Remove-Item -LiteralPath "D:\water-main (1)\water-main\.runtime\*.log" -Force
```

- 数据库、账号、历史数据都在 MySQL 里，删 `.venv` 不会丢数据；
- 想换回旧版本：先停掉当前服务，再到 `D:\water-main\water-main\backend` 按同样方式启动
  （⚠️ 两份副本共用同一个 `iot_system` 库，表结构会被旧版本再次迁移，切回前建议先备份数据库）；
- 想彻底清理数据库：用 root 执行 `DROP DATABASE iot_system;`（⚠️ 不可恢复，未在本机执行）。

---

## 11. 附录 A：命令速查表

| 目的 | 命令 |
| --- | --- |
| 启动（前台） | `cd "D:\water-main (1)\water-main\backend"; & "..\.venv\Scripts\python.exe" main.py` |
| 启动（后台，写日志） | 见 [4.5 方式二](#45-启动后端) |
| 停止 | `Stop-Process -Id (Get-NetTCPConnection -LocalPort 8000 -State Listen).OwningProcess -Force` |
| 是否在运行 | `Get-NetTCPConnection -LocalPort 8000 -State Listen` |
| 看业务日志 | `Get-Content "D:\water-main (1)\water-main\.runtime\uvicorn-err.log" -Encoding UTF8 -Tail 20` |
| 看访问日志 | `Get-Content "D:\water-main (1)\water-main\.runtime\uvicorn-out.log" -Encoding UTF8 -Tail 20` |
| 一键体检 | 见 [4.6](#46-验收) |
| 数据库是否正常 | `Get-Service MySQL80` |
| 设备是否可达 | `Test-NetConnection 192.168.31.100 -Port 80 -InformationLevel Quiet` |
| 三条测试 | 见 [8.1](#81-三条测试命令串行执行) |
| 浏览器无头自查 | 见 [7.10](#710-页面还是旧界面--白屏) |
| 更新到最新版本 | 见 [5.6](#56-更新到最新版本拉取远端仓库) |

---

## 12. 附录 B：本次实测记录

实测时间 2026-09-18（Asia/Shanghai），环境同上。

| # | 场景 | 执行内容 | 结果 |
| --- | --- | --- | --- |
| 1 | 环境检查 | `python --version`、`Get-Service MySQL80` | ✅ 3.12.9 / Running |
| 2 | 依赖安装 | `python -m venv .venv` + `pip install -r backend\requirements.txt` | ✅ Successfully installed（fastapi 0.141.1、uvicorn 0.53.0、pymysql 1.2.3、DBUtils 3.2.0） |
| 3 | 依赖隔离 | 打印 `sys.prefix` / `fastapi.__file__` | ✅ 均指向 `.venv` |
| 4 | 数据库连通 | `mysql -u iot_user -piot_pass_2026 -D iot_system -e "select 1;"` | ✅ 账号与库可用（含 5 张业务表） |
| 5 | 前台启动 + Ctrl+C | `python main.py`，观察启动日志后 Ctrl+C | ✅ 启动打印设备地址与通道；Ctrl+C 干净退出 |
| 6 | 后台启动 | `Start-Process … uvicorn`，写 `.runtime` 日志 | ✅ 1~5 秒内监听 8000 |
| 7 | 根目录启动 | `python backend\main.py` | ✅ 正常监听并托管前端 |
| 8 | 端口冲突 | 重复启动同一端口 | ✅ 复现 `[Errno 10048]`，进程自行退出 |
| 9 | 虚拟环境缺失 | 临时改名 `.venv` 后启动 | ✅ 复现「不是可识别的 cmdlet」报错，恢复后正常 |
| 10 | 数据库未启动 | `IOT_DB_PORT=3307` 启动 | ✅ 复现 `2003 / WinError 10061` + `Application startup failed` |
| 11 | 数据库密码错 | `IOT_DB_PASSWORD=wrong_password` | ✅ 复现 `1045 Access denied` |
| 12 | 权限不足 | 用 `iot_user` 执行建库/建号 SQL | ✅ 复现 `1227 / 1044`（语句语法本身有效） |
| 13 | 配置覆盖 | `WATER_DEVICE_URL` / `WATER_SAMPLE_PERIOD` | ✅ 生效，代理白名单自动加入设备主机 |
| 14 | 一键体检 | 4.6 的脚本原样执行 | ✅ 200 / 200 / 数据库 True / 设备离线 False |
| 15 | 局域网访问 | 请求 `http://192.168.31.170:8000/` | ✅ 200（跨设备访问需放行防火墙 8000，见 7.7） |
| 16 | 停止服务 | 端口占用者一行命令结束 | ✅ 端口立即释放、无残留 Python 进程 |
| 17 | 重启服务 | 停止 → 启动 | ✅ 正常，数据与布局保持 |
| 18 | 忘记密码找回 | SQL 写入新哈希 → 登录 → 还原 | ✅ 新密码 `code=0`，还原后 `admin123` `code=0` |
| 19 | 开机自启（启动文件夹） | 写入并运行 `.cmd` | ✅ 6 秒内监听 8000、`/docs` 200；已删除（未保留自启） |
| 20 | 开机自启（计划任务） | 普通权限注册 | ⚠️ 被拒（需管理员），已给出管理员命令 |
| 21 | 页面渲染自查 | 无头 Edge `--dump-dom` | ✅ 抓到登录卡片 DOM |
| 22 | 单元测试 | `test_level.py` | ✅ 全部通过 |
| 23 | 接口测试 | `test_api.py` | ✅ 146 通过 / 0 失败 / 4 提示（设备在线） |
| 24 | 端到端测试 | `node tests\e2e_web.js` | ✅ 151 通过 / 0 失败 / 0 提示，无控制台报错 |
| 25 | E2E 并发故障 | 与接口测试并发执行 | ✅ 复现失败，确认须串行；清理残留浏览器后重跑通过 |
| 26 | 现场设备在线链路 | 读取真实流量/温度并开泵 | ⚠️ 设备当前离线，需现场上电后确认 |
| 27 | 拉取远端更新 | 克隆 `github.com/giaoLRL/water` 与本地逐文件比对（走本机代理 7897） | ✅ 43 个文件与远端 `839af1f` 一致；本地独有的 `部署指南.md`、`.venv`、`.runtime` 均保留 |
| 28 | 更新后回归 | 停旧实例 → 新代码重启 → 三条测试串行执行 | ✅ 新表 `custom_sensor_data` 自动创建；测试全绿（141 / 145） |
| 29 | 控制卡布尔源 + 下发回读 | 临时卡片指向设备只读 `/api/data`（布尔字段）+ 本机只读指令 URL，无头浏览器实测 | ✅ 8/8：布尔源正确显示已开/已关、点击后立即回读（+2 次取数）、开关以真实状态为准 |
| 30 | 「卡片即来源」重构 | 删除执行器档案与通道声明；联动触发源/动作改选实时监控页卡片 | ✅ 无头浏览器实读下拉：触发源=瞬时流量/累计水量/双温/水压/光照/双水槽，动作=水泵开关/加热开关/定量浇水/水泵2（自建卡） |

> 未在本机验证的两项（#20 计划任务、#26 现场设备链路）都已给出验证方法；
> 其余全部命令均为实测可执行。
