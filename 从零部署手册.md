# 从零部署手册（全新 Windows 电脑 · 无环境 · 无代理 · 无网络）

> 面向场景：**一台刚装好系统的 Windows 电脑**，没有 Python、没有 MySQL、没有 Node.js，
> 也**没有代理/外网**，要用 U 盘（或提前拷好的安装包）把本系统完整跑起来。
>
> 本手册的命令都是**在这台开发机上实测执行过**的（2026-09-18 实测，环境：Windows 11 +
> Python 3.12.9 + MySQL 8.0.46）。标记含义：
>
> | 标记 | 含义 |
> | --- | --- |
> | ✅ | 本机实测通过，命令可直接复制 |
> | ⚠️ | 需要"全新电脑"或管理员权限才能验证，已给出判断标准与替代做法 |
>
> 相关文档：[部署指南.md](部署指南.md)（日常启停、故障排查、验收清单）、[README.md](README.md)（功能与接口）。

---

## 0. 30 秒结论（老手只看这段）

```powershell
# 0) 装好 Python 3.11+ 与 MySQL 8，并把代码放到 D:\water-main
# 1) 建库建账号（root 执行，见第 5 步）
# 2) 建虚拟环境 + 装依赖（无网见第 6.3 节）
cd "D:\water-main"
python -m venv .venv
& ".\.venv\Scripts\python.exe" -m pip install -r backend\requirements.txt

# 3) 一键自检（会告诉你还缺什么，并把结论写进 .runtime\startup.log）
& ".\.venv\Scripts\python.exe" backend\preflight.py

# 4) 启动
cd backend
& "..\.venv\Scripts\python.exe" main.py
```

浏览器打开 <http://127.0.0.1:8000>，账号 `admin` / `admin123`。
**任何一步失败都会打印「[FAIL] 项 + ↳ 处理建议」并写入 `.runtime\startup.log`**（第 7 步）。

---

## 1. 动手前：U 盘要带什么

全新电脑没有网络，**先把下面这些拷到 U 盘**（有网时提前下好，与目标机位数一致：一般 64 位）：

| # | 文件 | 从哪里拿 | 必需性 |
| --- | --- | --- | --- |
| 1 | `python-3.12.x-amd64.exe` | python.org → Downloads → Windows installer (64-bit) | 必需 |
| 2 | MySQL 8 安装包（`mysql-installer-community-8.0.x.msi`，约 300MB+） | dev.mysql.com → MySQL Installer for Windows | 必需 |
| 3 | 本项目代码（GitHub 的 `Code → Download ZIP`，或整个仓库文件夹） | github.com/giaoLRL/water | 必需 |
| 4 | Python 依赖 wheel 包文件夹（第 6.3 节生成，约 5MB） | 任意一台**有网**的电脑上执行 `pip download` | 无网时必需 |
| 5 | （可选）`node-v2x.x.x-x64.msi` | nodejs.org | 仅"前端端到端测试"需要，不影响系统运行 |
| 6 | （可选）本手册与《部署指南.md》 | 仓库里自带 | 方便对照 |

> ⚠️ **别把开发机上的 `.venv` 目录拷过去**：虚拟环境里写死了绝对路径，拷到别的机器/别的目录会失效。
> 到新机器上重新 `python -m venv .venv` 再装依赖即可（第 6 步）。

---

## 2. 第 1 步：安装 Python（⚠️ 需在本机手动装一次）

1. 双击 `python-3.12.x-amd64.exe`；
2. **勾选最下面的 `Add python.exe to PATH`**（这一步不勾，后面 `python` 命令会找不到）；
3. 点 `Install Now`，等待完成；
4. 关掉安装窗口，**重新打开**一个 PowerShell 窗口（PATH 才会生效）。

验证（✅ 实测输出示例）：

```powershell
python --version        # 期望：Python 3.12.9
where.exe python        # 期望：打印 python.exe 的完整路径
pip --version
```

**判断标准**：`python --version` 显示 **3.11 或更高**。

- 如果提示"`python` 不是内部或外部命令"：PATH 没生效 → 重开窗口；仍不行就重新运行安装包选
  `Modify` → 勾选 `Add Python to environment variables`；
- 如果显示 3.10 或更低：后端自检会直接判失败（需要 3.11+），请装新版。

---

## 3. 第 2 步：安装 MySQL 8（⚠️ 需在本机手动装一次）

1. 双击 `mysql-installer-community-8.0.x.msi`，选 **`Server only`**（只要数据库服务，约 400MB）
   或 `Custom` 只勾 `MySQL Server 8.0`；
2. 一路 `Next`，到 **`Accounts and Roles`** 这一步：
   - **设置 root 密码并记下来**（例如 `Root_2026!`）—— 后面建库要用；
   - 不要点 `Add User`（业务账号我们后面用 SQL 建，见第 5 步）；
3. 到 **`Windows Service`** 这一步：
   - **保持勾选 `Configure MySQL Server as a Windows Service`**；
   - `Start the MySQL Server at System Startup` 勾上（开机自启，比赛机器推荐）；
   - 服务名默认 **`MySQL80`**（记住它，下面命令要用）；
4. 完成后在 `Apply Configuration` 页应全绿。

验证（✅ 实测输出示例）：

```powershell
Get-Service MySQL80 | Select-Object Name, Status      # 期望：Running
Get-NetTCPConnection -LocalPort 3306 -State Listen    # 期望：有 LISTENING（LocalAddress 0.0.0.0 或 127.0.0.1）
```

**判断标准**：服务 `Running` + 3306 在监听。

- 服务名不是 `MySQL80`（比如叫 `MySQL`、`MySQL84`）→ 后面命令里的服务名相应替换；
- 服务没起来：`Start-Service MySQL80`（管理员）；仍失败看 MySQL 错误日志（安装目录 `Data\*.err`）；
- 3306 被别的程序占用：安装时改端口（如 3307），并把 `backend/config.py` 的 `DB_PORT` 或环境变量
  `IOT_DB_PORT` 改成同一个值。

---

## 4. 第 3 步：放置项目代码

把仓库文件夹（或解压后的 zip）放到一个**路径不要太深、不要有中文**的目录，例如 `D:\water-main`。

```powershell
cd "D:\water-main"
Get-ChildItem          # 期望看到：backend  frontend  tests  scripts  部署指南.md  README.md  ...
Get-ChildItem backend  # 期望看到：main.py  config.py  api.py  preflight.py  requirements.txt  ...
```

**判断标准**：`backend`、`frontend`、`requirements.txt` 都在。缺文件说明拷贝不完整（U 盘中断/只拷了一部分），
重新拷一次；后端自检的"后端文件/前端静态文件"两项会直接指出缺什么。

> 也可以只拷 `backend` + `frontend` + `tests`（运行只需要这两个目录），
> 但建议整份拷贝，方便对照文档与跑测试。

---

## 5. 第 4 步：建数据库与业务账号（⚠️ 需要 root 密码）

MySQL 装好后**默认没有本项目的库和账号**，必须用 root 建一次（只需一次）。

```powershell
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql -h 127.0.0.1 -u root -p
```

输入 root 密码后，逐条执行（可直接整段粘贴）：

```sql
CREATE DATABASE IF NOT EXISTS iot_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
CREATE USER IF NOT EXISTS 'iot_user'@'127.0.0.1' IDENTIFIED BY 'iot_pass_2026';
CREATE USER IF NOT EXISTS 'iot_user'@'localhost' IDENTIFIED BY 'iot_pass_2026';
GRANT ALL PRIVILEGES ON iot_system.* TO 'iot_user'@'127.0.0.1';
GRANT ALL PRIVILEGES ON iot_system.* TO 'iot_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

验证（✅ 实测输出示例，注意 **`-h 127.0.0.1` 必须带**）：

```powershell
& $mysql -h 127.0.0.1 -u iot_user -piot_pass_2026 -D iot_system -e "select 1; show tables;"
```

期望：先输出一行 `1`，随后列出表名（第一次可能还没有表，后端启动时会自动建）。

**判断标准**：没有报 `1045 Access denied` / `1049 Unknown database`。

- 报 `1045`：账号密码不对 → 检查 `backend/config.py` 的 `DB_USER/DB_PASSWORD`；
- 报 `1049`：库没建 → 回上面执行 `CREATE DATABASE`；
- 用 `-u iot_user` 但不带 `-h 127.0.0.1` 会走 localhost 通道，可能与授权账号不匹配（本机实测过这个坑）。

---

## 6. 第 5 步：建虚拟环境 + 装依赖

### 6.1 建虚拟环境（✅ 实测）

```powershell
cd "D:\water-main"
python -m venv .venv
& ".\.venv\Scripts\python.exe" -c "import sys; print(sys.prefix)"     # 期望：D:\water-main\.venv
```

### 6.2 有网（或能连内网源）时装依赖（✅ 实测）

```powershell
& ".\.venv\Scripts\python.exe" -m pip install -r backend\requirements.txt
```

如果直连 pypi 很慢/超时，换国内镜像（✅ 实测可下载）：

```powershell
& ".\.venv\Scripts\python.exe" -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r backend\requirements.txt
```

### 6.3 **无网络/无代理**时装依赖（✅ 实测离线解析成功）

**第一步：在一台有网的电脑上**（可以是另一台笔记本/机房机器），把依赖下成 wheel 文件：

```powershell
cd "D:\water-main"                                   # 有网机器上的同一份代码
python -m venv .venv
& ".\.venv\Scripts\python.exe" -m pip download -r backend\requirements.txt -d wheels
```

实测会得到 21 个文件、约 5MB（示例：`fastapi-0.141.1-py3-none-any.whl`、`uvicorn-0.53.0-py3-none-any.whl`、
`pymysql-1.2.3-py3-none-any.whl`、`dbutils-3.2.0-py3-none-any.whl` 等）。
把整个 **`wheels` 文件夹**拷到 U 盘。

> 注意：wheel 与 **Python 版本 + 系统位数**绑定（本次抓到的是 `cp312-win_amd64`）。
> 目标机装同版本 Python（3.12 64 位）最省事；版本不同就在目标机上执行
> `python -m pip download -r requirements.txt -d wheels --python-version 3.12 --only-binary=:all:`
> 生成对应版本（此命令未在无网机验证，属标准 pip 用法）。

**第二步：在无网的目标机上**，把 `wheels` 放到项目根目录，然后：

```powershell
cd "D:\water-main"
python -m venv .venv
& ".\.venv\Scripts\python.exe" -m pip install --no-index --find-links wheels -r backend\requirements.txt
```

实测（用 `--dry-run` 验证解析，输出 `Would install ...`，退出码 0）：

```text
Would install DBUtils-3.2.0 PyMySQL-1.2.3 PyYAML-6.0.3 annotated-doc-0.0.5 ... uvicorn-0.53.0 ...
```

### 6.4 验证依赖（✅ 实测）

```powershell
& ".\.venv\Scripts\python.exe" -c "import fastapi, uvicorn, pymysql, dbutils; print('依赖 OK')"
```

**判断标准**：打印 `依赖 OK`。报 `ModuleNotFoundError` 说明上一步没装全（多半是在别的解释器里装的，
或 U 盘里的 wheels 不全）。

---

## 7. 第 6 步：一键自检（**新加的能力，强烈建议每次都跑**）

```powershell
cd "D:\water-main"
& ".\.venv\Scripts\python.exe" backend\preflight.py
```

它会按顺序检查 **Python 版本 → 虚拟环境 → 第三方依赖 → 后端文件 → 前端静态文件 → 端口 → 数据库 →
现场设备可达性**，每项给出 `[ OK ] / [WARN] / [FAIL]`，失败项直接给出**可复制的修复命令**，
结果同时写入 **`.runtime\startup.log`**（带时间戳，追加）。

本机实测通过时的输出（节选）：

```text
2026-09-18 20:54:59 | 启动自检  |  主机 MSI  |  Python 3.12.9
2026-09-18 20:54:59 | [ OK ] Python 版本      3.12.9 (D:\water-main (1)\water-main\.venv\Scripts\python.exe)
2026-09-18 20:54:59 | [ OK ] 虚拟环境           正在使用项目虚拟环境 D:\water-main (1)\water-main\.venv
2026-09-18 20:54:59 | [ OK ] 第三方依赖          FastAPI 框架 / ASGI 服务器 / MySQL 驱动 / 数据库连接池 均已安装
2026-09-18 20:54:59 | [ OK ] 后端文件           ...\backend 必需文件齐全
2026-09-18 20:54:59 | [ OK ] 前端静态文件         ...\frontend（含 index.html / js/app.js）
2026-09-18 20:54:59 | [ OK ] 数据库连接          MySQL 8.0.46 @ 127.0.0.1:3306，账号 iot_user 可用
2026-09-18 20:54:59 | [ OK ] 数据库            iot_system 已存在（表结构会在启动时自动建/迁移）
2026-09-18 20:54:59 | [WARN] 现场设备           http://192.168.31.100 当前不可达（URLError: ...）
2026-09-18 20:54:59 |         ↳ 不影响后端启动：界面会显示「离线」、读数按真实模式给 --/0。...
2026-09-18 20:54:59 | 结论：自检通过（7 项正常 / 1 项提示 / 0 项失败）
```

**退出码**：全部通过 `0`，有失败项 `2`（脚本里可据此判断，例如 CI/U 盘自检脚本）。

---

## 8. 第 7 步：启动并打开页面

### 8.1 前台启动（推荐先用它确认一切正常）✅

```powershell
cd "D:\water-main\backend"
& "..\.venv\Scripts\python.exe" main.py
```

正常时启动日志长这样（✅ 实测）：

```text
... 结论：自检通过（7 项正常 / 1 项提示 / 0 项失败）
... 启动初始化完成：数据库/建表/账号/采集线程就绪
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
... [SENSOR] flow=0.00 L/min total=0.000 L pump=off ...
```

**换端口**（8000 被占用时）：`& "..\.venv\Scripts\python.exe" main.py 8010`，
或先设环境变量 `$env:WATER_PORT="8010"`。
这个窗口按 `Ctrl+C` 停止服务。

### 8.2 后台启动（写日志，关掉窗口不影响）✅

```powershell
$repo = "D:\water-main"
Start-Process -FilePath "$repo\.venv\Scripts\python.exe" `
  -ArgumentList "-m","uvicorn","main:app","--host","0.0.0.0","--port","8000" `
  -WorkingDirectory "$repo\backend" -WindowStyle Hidden `
  -RedirectStandardOutput "$repo\.runtime\uvicorn-out.log" `
  -RedirectStandardError  "$repo\.runtime\uvicorn-err.log"
```

### 8.3 打开页面与验收 ✅

| 项目 | 地址 / 值 |
| --- | --- |
| 前端页面 | <http://127.0.0.1:8000>（登录页即首页） |
| 接口文档 | <http://127.0.0.1:8000/docs> |
| 默认账号 | `admin` / `admin123` |

```powershell
"--- 一键体检（本机三连）---"
Get-NetTCPConnection -LocalPort 8000 -State Listen      # 有 LISTENING 即服务在跑
(Invoke-WebRequest http://127.0.0.1:8000/ -UseBasicParsing).StatusCode        # 期望 200
(Invoke-WebRequest http://127.0.0.1:8000/docs -UseBasicParsing).StatusCode    # 期望 200
```

---

## 9. 第 8 步：让别的设备（手机/另一台电脑）也能访问

⚠️ 需要管理员 PowerShell 执行一次（放行入站端口，`-Profile Any` 同时覆盖域/专用/公用）：

```powershell
New-NetFirewallRule -DisplayName "Water IoT 8000" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow -Profile Any
```

然后**别的设备**上用本机的局域网 IP（**不是 127.0.0.1**）访问：

```text
http://<本机WLAN的IPv4地址>:8000/
```

```powershell
ipconfig                      # 看「无线局域网适配器 WLAN」的 IPv4 地址
```

排查细节见《部署指南.md》7.7（含"为什么 127.0.0.1 在别的设备上打不开"、
防火墙程序规则按 exe 路径匹配、AP 隔离等 6 项自查）。

---

## 10. 第 9 步：验收测试（可选，交付/比赛前建议全跑）

前提：后端已在运行。**串行**执行，别并发：

```powershell
cd "D:\water-main"
$py = ".\.venv\Scripts\python.exe"

# 1) 部署自检（不需要数据库以外的依赖，最先跑）
& $py backend\preflight.py

# 2) 液位/纯真实模式单元测试（不联网、不连数据库）
& $py backend\tests\test_level.py

# 3) 部署自检模块的单元测试（端口/依赖/错误分类/日志）
& $py backend\tests\test_preflight.py

# 4) 后端接口功能测试（需后端已启动；不点水泵、不清零）
& $py backend\tests\test_api.py

# 5) 前端端到端测试（需 Node.js；会真实打开无头浏览器）
node tests\e2e_web.js
```

本机实测结果（2026-09-18；**设备在线/离线各跑过一次**，离线时设备相关断言按设计降级为提示）：

| 测试 | 结果 |
| --- | --- |
| `backend\preflight.py` | 自检通过（7 项正常 / 1 项提示 / 0 项失败） |
| `backend\tests\test_level.py` | 全部通过 |
| `backend\tests\test_preflight.py` | **18 通过 / 0 失败** |
| `backend\tests\test_api.py` | 设备在线 **211 通过 / 0 失败 / 4 提示**；设备离线 **205 通过 / 0 失败 / 10 提示** |
| `tests\e2e_web.js` | 设备在线 **248 通过 / 0 失败 / 0 提示**；设备离线 **243 通过 / 0 失败 / 4 提示** |

> 三条测试都不会启动水泵、不会清零累计水量；E2E 结束会自动把仪表盘布局还原（并核对卡片数）。
> 离线时的提示都是"设备离线/无自定义执行器时的设计内降级"，**不是失败**；闭环用例已改为不依赖
> 现场设备（温度来源卡读本机 `/api/water/realtime` 的 `data.period`），因此断着设备也能跑通。

---

## 11. 第 10 步：比赛/演示机器的两项加固

1. **开机自启**：见《部署指南.md》9.1（启动文件夹，无需管理员）或 9.2（计划任务，需管理员）；
2. **不让电脑自动睡**：见《部署指南.md》9.3（`powercfg /change standby-timeout-ac 0` 等，
   本机已设，另注意该机型"合盖"行为无法用 powercfg 关掉，演示时开盖）。

---

## 12. 故障速查表（症状 → 操作）

| 症状 | 原因 | 处理 |
| --- | --- | --- |
| `python` 不是命令 | Python 没装或 PATH 没生效 | 见第 2 步；重开 PowerShell；或用完整路径 |
| `pip` 装依赖时超时/报 SSL | 无外网 | 走第 6.3 节离线 wheel；或换清华镜像 6.2 |
| 自检 `[FAIL] 第三方依赖` | 依赖没装/装到别的解释器 | `& ".\.venv\Scripts\python.exe" -m pip install -r backend\requirements.txt` |
| 自检 `[FAIL] Python 版本 3.10` | 版本过低 | 装 3.11+，删掉 `.venv` 重建（`python -m venv .venv`） |
| 自检 `[FAIL] 数据库连接 (2003 ... 10061)` | MySQL 服务没启动/端口不对 | `Get-Service MySQL80` → `Start-Service MySQL80`；或改 `IOT_DB_PORT` |
| 自检 `[FAIL] 数据库连接 (1045)` | 账号或密码错 | 检查 `backend/config.py` 的 `DB_USER/DB_PASSWORD`，或用 root 重建账号（第 5 步） |
| 自检 `[FAIL] 数据库连接 (1049)` | 库不存在 | root 执行 `CREATE DATABASE iot_system ...` |
| 自检 `[FAIL] 端口 8000` | 端口被占用 | 按提示结束进程，或 `python main.py 8010` |
| 启动报 `[Errno 10048]` | 同上（端口冲突） | 同上 |
| 页面打不开/白屏，但 `/docs` 正常 | `frontend` 目录缺失或路径不对 | 补齐 `frontend`，或改 `config.FRONTEND_DIR` / `IOT_FRONTEND_DIR` |
| 页面能打开但数据全是 `--`、显示离线 | 现场设备没上电/不同网段 | 见《部署指南.md》7.6；不影响后端运行 |
| 别的设备打不开 | 用了 `127.0.0.1`，或防火墙/AP 隔离 | 见第 9 步 + 《部署指南.md》7.7 |
| 卡片被别的窗口覆盖 | 布局全局共享一份 | 编辑模式点「恢复上一次」，或 `POST /api/water/dashboard/layout/restore_prev` |
| 想看"到底哪一步失败" | —— | **永远先看 `.runtime\startup.log`**（启动自检报告，带时间戳） |

---

## 13. 附录 A：本次新增的"失败必留日志"能力

需求：**运行 `main.py` 时，只要前后端环境没配好，任何原因都要输出日志。** 实现方式：

| 机制 | 位置 | 说明 |
| --- | --- | --- |
| 启动自检 | `backend/preflight.py` | 只用标准库实现（依赖缺失这种失败也能查出来）；8 项检查，每项失败都给修复命令 |
| 日志落地 | `.runtime/startup.log` | 追加写、带时间戳；控制台同时打印 |
| 导入前置 | `backend/main.py` 顶部 | 在导入 fastapi 等业务依赖**之前**执行自检，失败 `exit(2)`，不再只抛 traceback |
| 初始化兜底 | `backend/main.py` 的 `lifespan` | 建表/账号/采集线程初始化的异常会写日志（含完整堆栈）后抛出 |
| 端口冲突 | `preflight.check_port()` | 启动前先试绑端口，报错时给出**占用进程 PID** 与两条处理命令 |
| 数据库错误分类 | `preflight._classify_db_error()` | 2003/1045/1049/1044/1040 分别给不同建议（启服务 / 建账号 / 建库 / 授权 / 连接数） |
| 一键体检 | `python backend/preflight.py` | 人可读报告，退出码 0/2，可写进现场自检脚本 |
| 单元测试 | `backend/tests/test_preflight.py` | 18 项断言（版本判断、依赖缺失、前端目录缺失降级、端口占用两种语义、5 类数据库错误、日志写入） |

实测覆盖的场景（都在本机跑过，退出码均为 2、日志均写入 `.runtime/startup.log`）：

| 场景 | 触发方式 | 实测输出要点 |
| --- | --- | --- |
| 端口被占用 | 服务已跑在 8000 时再启动一个 | `[FAIL] 端口 8000 无法绑定：[WinError 10048] ... 占用进程 PID=38864` + 结束进程/换端口两条建议 |
| MySQL 没启动/端口错 | `$env:IOT_DB_PORT="3307"` | `[FAIL] 数据库连接 连接不上 MySQL（iot_user@127.0.0.1:3307）(2003, ... 10061)` + 启服务/改端口建议 |
| 数据库密码错 | `$env:IOT_DB_PASSWORD="wrong_pass"` | `[FAIL] ... (1045, "Access denied ...")` + 建账号 SQL |
| 依赖缺失 | 全新空 venv 里跑 `main.py` | `[FAIL] 第三方依赖 缺少：FastAPI 框架...` + pip 命令 + 离线安装指引 |
| Python 版本过低 | 用本机 Python 3.10 跑 `main.py` | `[FAIL] Python 版本 3.10.11` + "需要 Python 3.11 或更高" |
| 前端目录缺失 | `$env:IOT_FRONTEND_DIR="...\不存在"` | `[WARN] 前端静态文件 目录不存在`（接口仍可用）+ 补齐建议 |
| 现场设备离线 | 设备断电（本机常态） | `[WARN] 现场设备 当前不可达` + "不影响后端启动" |

---

## 14. 附录 B：U 盘打包清单（照抄即可）

```text
U 盘\
├─ python-3.12.9-amd64.exe
├─ mysql-installer-community-8.0.46.msi
├─ node-v24.x.x-x64.msi                 （可选：只跑前端 E2E 才需要）
├─ water-main\                          （项目代码整份）
│   ├─ backend\  frontend\  tests\  scripts\
│   ├─ 从零部署手册.md  部署指南.md  README.md
│   └─ wheels\                          （无网机器必需：第 6.3 节生成）
└─ 数据库备份（可选）\
    └─ iot_system.sql                   （用 mysqldump 导出，想带着历史数据迁移时用）
```

> 想连历史数据一起搬：在有数据的机器上执行
> `& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" -h 127.0.0.1 -u iot_user -piot_pass_2026 iot_system > iot_system.sql`，
> 目标机建好库后 `& $mysql -h 127.0.0.1 -u iot_user -piot_pass_2026 -D iot_system < iot_system.sql`
> （⚠️ 该导出/导入未在本次实测范围内，属 MySQL 标准用法）。

---

## 15. 附录 C：全流程命令清单（按顺序复制）

```powershell
# ============ 1. 检查前置 ============
python --version                                  # 需要 3.11+
Get-Service MySQL80 | Select-Object Name, Status  # 需要 Running

# ============ 2. 代码与虚拟环境 ============
cd "D:\water-main"
python -m venv .venv

# 有网：
& ".\.venv\Scripts\python.exe" -m pip install -r backend\requirements.txt
# 无网（wheels 目录来自有网机器）：
# & ".\.venv\Scripts\python.exe" -m pip install --no-index --find-links wheels -r backend\requirements.txt

& ".\.venv\Scripts\python.exe" -c "import fastapi, uvicorn, pymysql, dbutils; print('依赖 OK')"

# ============ 3. 建库建账号（root，见第 5 步 SQL）============
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql -h 127.0.0.1 -u iot_user -piot_pass_2026 -D iot_system -e "select 1;"

# ============ 4. 自检 ============
& ".\.venv\Scripts\python.exe" backend\preflight.py

# ============ 5. 启动 ============
cd backend
& "..\.venv\Scripts\python.exe" main.py
# 后台启动见 8.2；换端口：main.py 8010

# ============ 6. 验收（另开一个窗口，后端需保持运行）============
cd "D:\water-main"
& ".\.venv\Scripts\python.exe" backend\tests\test_level.py
& ".\.venv\Scripts\python.exe" backend\tests\test_preflight.py
& ".\.venv\Scripts\python.exe" backend\tests\test_api.py
node tests\e2e_web.js

# ============ 7. 局域网放行（管理员 PowerShell，一次即可）============
New-NetFirewallRule -DisplayName "Water IoT 8000" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow -Profile Any
ipconfig        # 找到本机 WLAN 的 IPv4，别的设备访问 http://<该IP>:8000/
```
